<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this only avoids a mismatched flash before the first frame. -->
    <style name="Theme.Rem" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">#F7F7FA</item>
        <item name="android:statusBarColor">#F7F7FA</item>
    </style>
</resources>

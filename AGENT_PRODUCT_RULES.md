# Rem 当前产品与 Agent 工作规范

## 1. 当前目标

Rem 是一个本地图片、漫画和视频查看工具。当前阶段只建立最小、可验证的本地文件接入和浏览能力，并在应用内提供查看与播放（规则见 5.1）；Agent 必须围绕已确认行为工作，不为未来功能预先实现复杂基础设施。

本文件是当前会话确认后的有效规范。与本文件冲突的旧产品描述、旧分类模型、旧页面流程和旧需求均不再作为置信依据。

## 2. Library 根目录

Library 根目录只允许以下目录：

```text
Library/
  相册/
  画集/
  待分类/
  .gallery/
```

- `相册/` 和 `画集/` 是当前可浏览内容；`相册/` 保持扁平，`画集/` 支持多级目录。
- `待分类/` 只用于暂存文件，当前不扫描、不索引、不展示、不整理、不提供任何功能。实际的文件归位由人完成。
- `.gallery/` 是 Rem 的可重建内部状态，按**软件缓存**定性：Rem 可以在这里写入、重建自己的文件；它不得替代原始媒体文件，也不受「媒体只读」约束。当前包含：
  - `RULES.md`：文件模式说明，供人或 AI 整理底层文件时作为依据；每次接入 Library 都用应用内置的最新版本覆写；
  - `index.json`：应用自己的扫描缓存，可随时删除并重建；
  - `thumbs/`：缩略图缓存，可随时删除，下次浏览时按需重建；该目录不进入系统相册。
- 不允许在根目录增加其他业务目录，也不允许根目录直接放媒体文件。

## 3. 相册文件模式

`相册/` 下直接放入图片和视频文件，不建立作品子文件夹。图片和视频可以混合查看，也可以分别查看。

排序时间按以下顺序取得：

1. 图片的 EXIF 拍摄时间或视频自身的拍摄时间元数据；
2. 没有可用拍摄时间时，使用文件修改时间。

地点只读取媒体已有的 GPS 或地点元数据；没有地点证据时不显示地点。当前不根据文件名、目录名或网络信息推断地点。

相册文件名当前不设强制命名格式。

## 4. 画集文件模式

`画集/` 第一层只能是严格命名的项目文件夹；项目文件夹内**允许继续建立子文件夹，层级不限**：

```text
画集/
  作者名称-项目名称/
    0001.jpg
    任意名称.png
    任意名称.mp4
    子文件夹/
      任意名称.jpg
```

- 第一层文件夹命名为“作者名称-项目名称”。作者名称和项目名称均为必填文本。
- 第一层以下的子文件夹命名自由，不再影响作者/项目的识别。
- 任意层级内的媒体文件命名自由；图片与视频可以放在同一层，也可以分开放。
- 不再要求四位数字序号，也不再定义缺号顺延、命名违规等规则。
- 当前不实现任何底层文件改名、移动、删除或覆盖功能；整理仍由人按 `.gallery/RULES.md` 完成。

### 4.1 排序与浏览

- `相册/` 固定按时间顺序浏览（拍摄时间优先，缺失时用修改时间）。
- `画集/` 提供可选排序方式，选择被记住：
  - **序号**：从文件名中取出数字组作为序号（例如 `01.jpg`、`zz001_002.jpg`），取不到序号的文件排在最后；同序号按名称自然序。
  - **名称**：自然序，`1.jpg` 排在 `2.jpg` 前、`10.jpg` 排在 `2.jpg` 后。
  - **拍摄时间**、**修改时间**、**文件大小**。
- 排序只影响显示，不改变文件在磁盘上的位置或名称。
- 同层内子文件夹与媒体文件不同时显示：进入一个文件夹后，先看到该层的子文件夹，再进入下一层查看其媒体内容。

## 5. 浏览行为

- 相册按时间顺序浏览。
- 画集最外层展示所有项目文件夹的项目名称；支持按作者名称搜索。进入后可以继续进入子文件夹，层级不限。
- 作者和项目名称均支持最小字符串匹配，输入内容出现在目标文本任意位置即可，例如 `abcde` 可匹配 `abc` 或 `cde`。
- 所有较长列表提供右侧拖动滑块快速定位；拖动时同时显示当前位置的时间和地点（相册）或该文件的序号/名称（画集）。短列表可以隐藏滑块。
- 点击图片或视频在 Rem 内置查看器内打开，不跳转到其他应用；查看器行为见 5.1。
- 查看器始终提供“用其他应用打开”作为显式操作，打开失败时也作为回退，因此系统查看器/播放器路径始终保留。
- 应用不做任何底层文件改名、移动、删除或覆盖；发现不符合目录规范的内容只在界面报告。

### 5.1 内置查看器

点击媒体后在 Rem 内直接查看或播放，并可在同一次列表结果内左右滑动连续切换，不跳出应用。

- **入口**：点击网格或列表中的媒体进入查看器，从被点击的位置开始；图片和视频共用同一个查看器。
- **连续浏览**：左右滑动在**当前文件夹内**切换上一个或下一个媒体，混合图片与视频；到该文件夹的边界即停止，不跨目录。查看器只消费传入的当前列表，不重新扫描、不改写索引、不产生第二套排序。
- **图片**：支持双指缩放、拖动平移和双击放大/还原。缩放到 1 倍时单指横向滑动翻页；放大后单指滑动优先平移图片，不得在缩放状态下误翻页。
- **视频**：在应用内播放，提供播放/暂停、进度拖动和时间显示。不支持滑动的格式（如 FLV）只播放、不提供进度拖动。
- **音频与生命周期**：离开当前视频、切换到其他媒体，或应用进入后台时，必须立即停止播放并释放播放器，不得留下任何后台音频；返回该视频时停留在原位置且保持暂停，不自动播放。
- **默认不记忆**：查看器不记录播放进度，关闭查看器或退出应用后不保留任何观看位置。
- **失败可见**：格式不支持或解码失败时不得崩溃或静默黑屏，必须给出明确提示，并保留“用其他应用打开”。
- **不动媒体**：查看器不修改任何媒体文件，也不写 `.gallery/` 之外的任何位置；这一点与第 9 节的 `.nomedia` 相互独立。

内置查看器的实现必须遵守四条已实测得出的硬规则：

1. 图片请求不得使用原始尺寸，否则大图会解码失败；解码尺寸交给图片库自行决定。见 `docs/STATUS.md` 的实测记录。
2. 视频使用单个播放器实例跨页复用，且随应用前后台创建与释放，不能只做暂停。
3. 失败必须是可见的正常控制流，不是异常状态。
4. 成功格式清单以真机与模拟器实测为准，不按扩展名承诺支持。

当前不为不常见格式做特殊兼容（例如引入额外的第三方解码库）。

## 6. 扫描边界

- 接入 Library 必须由用户显式选择目录触发；应用不自行选择目录。
- 只读取 `相册/` 和 `画集/` 的规定层级，不递归进入更深层级。
- 禁止自动全盘扫描、后台全盘扫描和读取 `待分类/` 内容。
- 接入后先直接展示 `.gallery/` 中的缓存，再在后台做增量扫描；扫描不阻塞阅读。
- 增量扫描只重读 `size` 或修改时间发生变化的文件，其余沿用缓存。
- 当前只建立读取、排序、筛选和查看所需的最小索引。

## 7. Agent 协作规则

- Agent 开工前先读取本文件、`docs/PRODUCT.md`、`docs/STATUS.md`，声明目标、范围、非目标、文件归属和验证方法。
- Agent 不得自行增加目录类型、媒体关系、Tag、Collection、自动识别、自动整理、阅读进度、复杂页面跳转或其他未确认功能。
- 多 Agent 只用于文件归属清晰且可独立验收的任务；共享规范由主 Agent 统一修改。
- 发现与本规范冲突的旧代码或旧文档时，先登记差距，不据此扩展目标。
- 应用只报告不符合目录规范的内容，不自动修正；实际整理由人完成。
- `.gallery/RULES.md` 是文件模式的唯一说明，由构建从仓库根目录 `AGENT_LIBRARY_RULES.md` 复制，不得在应用内另写一份。
- 任何底层文件改动功能都必须等待新的明确需求、预览、冲突检查、用户确认和可恢复方案；当前阶段不得实现。唯一例外是第 9 节确认的 `.nomedia` 控制标记，它不涉及任何媒体文件。

## 8. 当前明确暂停

以下内容直接清理出当前有效目标：旧的 Tag、Collection、复杂 Work/Series/Edition 关系、自动识别和自动整理、阅读进度、复杂页面跳转、后台全盘扫描，以及为未来文件编辑预先建设的大量功能。

内置查看器原在本清单中，已于 2026-09-20 作为新的明确要求重新确认并移出，规则见 5.1。本清单的其余内容未变。

后续若重新提出其中任何功能，必须作为新的明确要求重新确认。

## 9. 系统相册隐藏

设置中的“从系统相册隐藏”只管理 Library 根目录的 `.nomedia`：

- 开启时创建 `Library/.nomedia`；关闭时移除该文件；重复开关不产生 `.nomedia (1)` 之类的副本。
- `.nomedia` 是 Rem 唯一允许写入 Library 业务目录的控制文件；`相册/`、`画集/` 内的媒体始终只读。
- 开关状态由当前 Library 中 `.nomedia` 是否存在推导，不另存本机偏好，避免状态漂移。
- 不删除、不改名、不移动、不覆盖任何原始媒体，也不主动清除 `MediaStore` 已有记录。
- 对已经进入系统相册的媒体，仅创建 `.nomedia` 不等于立即隐藏；界面必须说明这一边界，不得宣称无条件隐藏成功。
- 未接入 Library 时开关不可用，不得静默失败。

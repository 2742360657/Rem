# Rem 当前产品与 Agent 工作规范

## 1. 当前目标

Rem 是一个本地图片、漫画和视频查看工具。当前阶段只建立最小、可验证的本地文件接入和浏览能力，Agent 必须围绕已确认行为工作，不为未来功能预先实现复杂基础设施。

本文件是当前会话确认后的有效规范。与本文件冲突的旧产品描述、旧分类模型、旧页面流程和旧需求均不再作为置信依据。

## 2. Library 根目录

Library 根目录只允许以下目录：

```text
Library/
  相册/
  画集/
  待分类/
  .gallery/
```

- `相册/` 和 `画集/` 是当前可浏览内容。
- `待分类/` 只用于暂存文件，当前不扫描、不索引、不展示、不整理、不提供任何功能。
- `.gallery/` 是 Rem 的可重建内部状态；不得替代原始媒体文件。
- 不允许在根目录增加其他业务目录，也不允许根目录直接放媒体文件。

## 3. 相册文件模式

`相册/` 下直接放入图片和视频文件，不建立作品子文件夹。图片和视频可以混合查看，也可以分别查看。

排序时间按以下顺序取得：

1. 图片的 EXIF 拍摄时间或视频自身的拍摄时间元数据；
2. 没有可用拍摄时间时，使用文件修改时间。

地点只读取媒体已有的 GPS 或地点元数据；没有地点证据时不显示地点。当前不根据文件名、目录名或网络信息推断地点。

相册文件名当前不设强制命名格式。

## 4. 画集文件模式

`画集/` 第一层只能是严格命名的项目文件夹：

```text
画集/
  作者名称-项目名称/
    0001.jpg
    0002.png
    0003.mp4
```

- 文件夹命名为“作者名称-项目名称”。作者名称和项目名称均为必填文本。
- 文件夹内直接放媒体文件，禁止继续建立子文件夹。
- 规范媒体文件名为四位数字序号加原扩展名，从 `0001` 开始。
- 发现缺号时，后续规范文件可自动向后顺延；不得覆盖已有文件。
- 不符合规范的其他文件放在规范序号文件之后，排序方式暂不规定。
- 图片和视频可以混合查看，也可以分别查看。
- 当前不实现任何底层文件改名、移动、删除或覆盖功能；上述缺号规则只定义将来的整理语义。

## 5. 浏览行为

- 相册按时间顺序浏览。
- 画集最外层展示所有项目文件夹的项目名称；支持按作者名称搜索。
- 进入作者项目后展示具体媒体；作者和项目名称均支持最小字符串匹配，输入内容出现在目标文本任意位置即可，例如 `abcde` 可匹配 `abc` 或 `cde`。
- 所有较长列表提供右侧拖动滑块快速定位；拖动时同时显示当前位置的时间和地点（相册）或序号（画集）。短列表可以隐藏滑块。
- 图片和视频优先调用底层系统播放器/查看器。当前不内置备用播放器；系统没有可处理应用时只报告无法打开。

## 6. 扫描边界

- 接入或刷新 Library 必须由用户显式触发。
- 只读取 `相册/` 和 `画集/` 的规定层级。
- 禁止自动全盘扫描、后台扫描和读取 `待分类/` 内容。
- 当前只建立读取、排序、筛选和查看所需的最小索引。

## 7. Agent 协作规则

- Agent 开工前先读取本文件、`docs/PRODUCT.md`、`docs/STATUS.md`，声明目标、范围、非目标、文件归属和验证方法。
- Agent 不得自行增加目录类型、媒体关系、Tag、Collection、自动识别、自动整理、阅读进度、复杂页面跳转或其他未确认功能。
- 多 Agent 只用于文件归属清晰且可独立验收的任务；共享规范由主 Agent 统一修改。
- 发现与本规范冲突的旧代码或旧文档时，先登记差距，不据此扩展目标。
- 任何底层文件改动功能都必须等待新的明确需求、预览、冲突检查、用户确认和可恢复方案；当前阶段不得实现。

## 8. 当前明确暂停

以下内容直接清理出当前有效目标：旧的 Tag、Collection、复杂 Work/Series/Edition 关系、自动识别和自动整理、阅读进度、复杂页面跳转、内置播放器、后台全盘扫描，以及为未来文件编辑预先建设的大量功能。

后续若重新提出其中任何功能，必须作为新的明确要求重新确认。

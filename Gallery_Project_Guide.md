# Rem 产品与便携格式规范

> 本文是 Rem 的当前产品规范。历史实现过程只放在 `docs/DEV_LOG.md`，开发执行规则只放在 `AGENTS.md`，用户操作只放在 `docs/USER_GUIDE.md`。
>
> 当前项目处于首次稳定发布前。以本规范、当前代码和最近一次用户决定为准，不为未发布的试验格式长期保留兼容分支。

## 1. 文档职责

| 文件 | 唯一职责 |
| --- | --- |
| `README.md` | 项目入口、能力概览和构建入口 |
| `Gallery_Project_Guide.md` | 产品目标、领域语义、便携格式和路线 |
| `AGENTS.md` | Agent 必须遵守的开发与安全规则、当前工作状态 |
| `docs/ARCHITECTURE.md` | 已实现的软件结构、数据流和技术约束 |
| `docs/USER_GUIDE.md` | 当前版本真实可用的用户操作 |
| `docs/DEV_LOG.md` | 按时间记录问题、证据、修复、验证和遗留风险 |
| `CHANGELOG.md` | 面向版本的用户可见变化 |

同一规则只保留一个权威位置。其他文档可以链接它，但不要复制整段说明。

## 2. 产品目标

Rem 是面向移动硬盘和大容量本地目录的 Android 媒体库：

- 原媒体保持普通文件，可脱离 Rem 使用；
- Library 自带便携元数据，换手机、重建本机索引后仍能恢复人工决定；
- 新文件、未知格式和低置信识别先进入 Inbox；
- 图片、视频、写真集和混合媒体可以按目录或逻辑 Group 浏览；
- 漫画可以作为独立 Work，也可以加入有顺序的 Series；
- 同一 Work 的多个版本使用 Edition 表达，可以比较、虚拟合并，再由用户决定是否物理清理；
- Android 端只做可靠、廉价的初步识别，复杂整理交给有明确规则的本地 Agent；
- 交互参考 EhViewer 和 MT 管理器：信息密度合理、返回行为可预测、长按与批量操作靠近内容。

当前不做云同步、多用户、实时协作、在线流媒体、推荐系统、AI 内容识别、图片/视频编辑和 PC 客户端。

## 3. 三层数据

### 3.1 媒体层

用户选择的 SAF 目录中的真实图片、视频、目录和压缩包。扫描、识别、分组、改标题或切换视图不得改变这一层。

### 3.2 便携层

Library 根目录中的 `.gallery/` 是可携带的用户真相：

```text
GALLERY_LIBRARY.md
.nomedia
.gallery/
  library.json
  schema/v4.json
  items/catalog.json
  state/state.json
  state/inbox.json
  imports/
  transactions/
  backups/
```

- `library.json`：Library 身份及 Schema 版本；
- `schema/v4.json`：当前格式与可编辑边界；
- `catalog.json`：Asset、Work、Edition、Group、Series；
- `state.json`：阅读/观看进度和逻辑回收站；
- `inbox.json`：用户在 Inbox 做过的决定（接受、人工归类、忽略、已处理）及其证据和时间；
- `imports/`：程序维护的导入和派生来源清单；
- `transactions/`：可恢复的物理文件事务；
- `backups/`：格式转换和高风险操作前的元数据快照；
- `GALLERY_LIBRARY.md`：交给外部 Agent 的库内操作说明。

所有路径使用以 `/` 分隔的 Library 相对路径。禁止写入 Android URI、Windows 盘符、绝对路径或 `..`。

### 3.3 本机层

Android 私有 SQLite、扫描补全队列、日志和缓存都是可丢弃投影：

- 删除数据库后应能从媒体和 `.gallery/` 重建；
- 本机发现记录不能成为人工决定的唯一保存位置；
- 离线小预览不进入 `.gallery/`，也不替代原图。

## 4. Schema v4 领域模型

v4 使用单向关系，避免同一成员列表在多个地方重复。

### 4.1 Asset

Asset 是一个物理来源：

- 文件、目录、压缩包或系统相册复制来源；
- 保存相对路径、媒体类型、来源类型、可选副文件和内容指纹；
- 不保存标题、作者、标签或 Series；
- 一个 Asset 可以被 Edition 引用，但不能因为逻辑合并自动删除。

### 4.2 Work

Work 是用户实际命名、收藏、阅读或观看的逻辑对象：

- 保存类型、`domain`、标题、作者、标签、Collection、封面选择和收藏状态；
- 保存可编辑字段的 `field_sources`；
- 通过 Edition 取得物理内容；
- 不内嵌 Group 或 Series 成员列表。

### 4.3 Edition

Edition 表示同一 Work 的一个取得版本：

- 指向一个 Work；
- 按顺序引用一个或多个 Asset；
- 成员角色包括 `primary`、`page`、`image`、`video`、`bonus`、`cover`、`alternate`；
- 可表达目录版、压缩版、附带视频版或虚拟去重版；
- 比较 Edition 不等于删除任何来源。

### 4.4 Group

Group 用于“一起浏览”，成员是 Work：

- `media_set`：写真集、图片/视频混合组等；
- `manual_collection`：用户明确建立的浏览集合；
- 支持有序/无序、成员角色、自定义顺序和封面 Work；
- Group 不是 Series，也不表示“同一作者的所有作品”。

### 4.5 Series

Series 用于有先后关系的 Work：

- 成员可以使用 `sort_index`；
- 也可以使用季/集或卷/章；
- 成员允许不编号；
- 手动顺序优先；
- Series 标题和成员关系只在 Series 实体中保存。

### 4.6 关系方向

```text
Edition ──> Work
Edition ──> Asset
Group   ──> Work
Series  ──> Work
State   ──> Work
```

Work 可以指定首选 Edition，但不能复制 Edition 的 Asset 列表。删除 Work 时必须清理 Group、Series 和 State 中的引用；只有不再被 Edition 引用的 Asset 元数据可以一并移除。

## 5. 三个主区域

- **相册**（`album`）：系统相册或 `Photos/` 导入内容，按拍摄时间浏览。
- **图片 / 视频**（`classified`）：普通图片、视频、写真集和混合 Group，可按物理文件夹或逻辑组浏览。
- **漫画 / 阅读**（`works`）：独立漫画、动画/影视作品和 Series，强调阅读/观看顺序与进度。

`domain` 是便携决定，目录只是识别证据。改变区域不要求移动文件。

## 6. 扫描、Inbox 与识别

扫描分为：

1. 快速清点：只列目录并建立可浏览索引；
2. 信息补全：分批读取哈希、ZIP/CBZ 页数、ComicInfo 和拍摄信息，并保存本机检查点。

规则：

- 新识别媒体在用户确认前只出现在 Inbox；
- 不支持文件和结构模糊目录也必须可见，但不能伪装成可打开媒体；
- `.gallery/`、`.nomedia`、`.ehviewer`、`ComicInfo.xml`、事务文件和已登记 sidecar 不生成独立卡片；
- 文件夹名是证据，不是权威；`JM` 可能只是人工目录；
- Android 端只实现小而可测试的识别器；
- 识别困难或成本过高时保持未处理，交由用户或本地 Agent；
- 自动结果必须保留来源和置信依据，不能覆盖 `manual` 字段。

### 6.1 Inbox 决定

用户在 Inbox 做出的决定写入 `.gallery/state/inbox.json`，不依赖本机数据库：

- `accepted`：接受扫描建议，Work 进入普通视图，自动字段仍可由 Agent 更新；
- `classified`：在 Inbox 里人工改了归属；权威值仍是 Work 的 `domain`，决定记录只说明当时的选择；
- `ignored`：不再出现在普通视图，也不回到 Inbox；媒体保持原样；
- `handled`：只用于没有 Work 的待判断路径（不支持的格式、结构模糊目录），表示用户已确认无需 Rem 处理。

决定以 Library 相对路径为键，目标存在 Work 时同时记录 `work_id`；路径变化时按 `work_id` 继续匹配，重命名或 Organizer 移动不能让被忽略内容复活。撤销决定只删除记录，不修改 Work 元数据。证据（扫描原因）只作为当时依据保存，不是分类结论。

## 7. 元数据来源与合并

推荐来源优先级：

```text
manual > 用户确认的 Library 规则 > provider > ComicInfo > 文件名/目录 > 默认值
```

这是“字段级”优先级，不是整条记录覆盖顺序。

- `field_sources.<field> == manual` 时，自动流程不得修改该字段；
- `tags` 当前按整组保护，人工改过后不得自动追加或去重；
- 稳定来源标签如 `source:jm`、`jm:album:<id>`、`source:ehviewer`、`eh:gid:<id>`、`source:pixiv`、`pixiv:id:<id>` 必须保留；
- 外部 Agent 的可靠结果标记为 `provider:<id>`；
- 只有用户明确指定的值才标记为 `manual`；
- 低置信、多候选或来源不可访问时保持原值并请求确认；
- Cookie、Token 和账号信息不得写入 Library、日志或 Git。

## 8. 分组、追加与合并

### 8.1 加入新内容

向已有画集加入新内容时，优先修改 Group 成员或 Edition 的 Asset 引用，不移动媒体。只有用户明确要求统一物理目录时，才生成 Organizer 计划。

### 8.2 多版本

两个相似画集先作为两个 Edition 保留：

1. 用户选择比较范围；
2. 对选定页面按需计算哈希；
3. 区分完全重复、独有内容和内容不同；
4. 预览虚拟合并结果；
5. 用户可以只保留虚拟统一视图；
6. 物理清理是独立、可恢复且再次确认的事务。

标题相似、同作者或同目录不能自动证明两个来源属于同一 Work。

比较必须按 I/O 代价分层。当前手机向移动硬盘顺序拷贝大视频实测可达约 360 MB/s；这不代表大量小文件、压缩包处理、哈希和 SAF/provider 往返也能达到相同速度：

1. **免费层**：只用索引里已有的页数、文件大小、修改时间和已完成的内容指纹；
2. **清单层（用户显式发起、可取消）**：为每个来源建立“页清单”——页名、每页字节大小，深度比较时再记录每页哈希。压缩包先复制到有界本机缓存，再用中央目录一次遍历清单；阅读器随后可随机访问条目，不得逐页重读整个压缩包；
3. **像素层（只对用户选中的少量页）**：降采样解码后比较内容，仅用于识别“重编码但同一张”，且只能作为建议，不能自动认定重复；
4. 清单结果按（来源路径、大小、修改时间）缓存在本机；重复比较不得重新读取。

任何一层都不得修改媒体；物理清理永远是独立的、可恢复且再次确认的事务。

### 8.3 混合图片和视频

同一目录的图片集与直属视频先派生为一个浏览组；这层派生只是展示，不是用户决定。用户可以把它一次性保存为便携 Group，之后手动增删成员、调整顺序和指定封面。Group 成员可以跨目录，成员的 `sort_index` 就是列表顺序，角色包括 item、image、video、bonus、cover。

保存派生组使用由主 Work 推导的稳定 ID，因此同一个目录重复保存只会更新既有 Group，不会产生第二个。删除 Group 只删除“一起浏览”的关系；被移出或被删除的成员不会失去媒体、Edition 或 Work 元数据。

## 8.4 阅读与浏览交互

不同内容有不同的阅读习惯，交互按内容类型分工，而不是一套手势打天下：

| 内容 | 打开方式 | 手势与呈现 |
| --- | --- | --- |
| 单张照片 / 图片 | 相册或图片列表点开 | 沉浸黑底；双指围绕手指缩放、单指平移、双击在「适应屏幕 ↔ 2.5×」之间切换；**适应屏幕时不能平移**，放大后不能被拖出屏幕边界；未放大时单指滑动交给外层翻页 |
| 目录图片集 / ZIP / CBZ | 作品详情页「开始阅读」 | 竖向连续阅读，页与页之间无间隙；未放大时上下滚动翻页；放大后**锁定当前页**，页面不再随手指滚动，退出缩放恢复连续模式；点按切换沉浸（隐藏/显示系统栏与进度） |
| 系列（多话漫画） | 系列条目 → 话列表 | 系列是**一个入口**，先看话列表（含阅读进度），可「继续阅读」；读到一话末尾自动进入下一话；单话仍按上面的连续阅读方式 |
| 视频 | 视频列表 | 系统播放器，支持进度保存与恢复，缩放不适用 |

原则：

- 阅读器的第一目标是**内容占满可用区域**，控件是覆盖层且可隐藏，不与页面抢空间；
- 手势必须可预期：能滚就不缩放、放大才平移、边界绝不越界；
- 几何计算（钳制、围绕焦点缩放、双击目标）是纯函数并有单测；真实手势用 Compose 手势测试在设备上验证，不靠肉眼；
- 参考成熟阅读器的既有模式（竖向连续 + 章节衔接的漫画阅读器、照片查看器的双指/双击/边界钳制），不自造一套需要用户重新学习的交互。

## 9. 文件安全

- 扫描、识别、编辑元数据、建立 Group/Series/Edition 都不得移动或删除媒体；
- 普通删除只写逻辑回收站；
- 永久删除前重新解析路径并核对大小/身份；
- Organizer 必须先生成计划和冲突报告；
- 复制完成并验证后才能删除源；
- 每一步记录到 `.gallery/transactions/`；
- 中断后必须能够继续或回滚；
- 高风险操作前备份便携元数据；
- 遇到高于客户端的 Schema 只读或拒绝写入。

## 10. 性能与缓存

- SAF 成本主要来自 provider/Binder 查询次数；
- 一个目录尽量只做一次带完整 projection 的查询；
- 复用路径定位、目录列表和未变化文件的补全结果；
- 大文件哈希只在用户选择比较、查重或合并时计算；
- 当前设备顺序拷贝大视频实测可达约 360 MB/s，但大量小文件、压缩包处理、哈希和 SAF/provider 往返仍可能成为主要瓶颈；按页哈希这类要读完整内容的操作仍须由用户显式发起、限定范围、可取消，并复用已完成结果；能用页数、条目大小和压缩包目录得到的结论不得读取字节；
- 阅读器按视口解码，不一次解码整部作品；
- 离线预览最长边 512 px，位于 `noBackupFilesDir`，限制为 256 MiB / 20,000 张并可清除；
- 压缩包阅读副本位于系统可回收的 App 缓存，通常限制为 512 MiB、按最久未使用清理，并可在设置中查看和清除；单个正在打开的大压缩包不能被自己的容量清理淘汰；
- 约 7.6 万条本机索引最终应分页或按需查询，不能长期依赖整表载入。

## 11. 当前实现状态

已经具备：

- Library 初始化、身份恢复、SAF 接入与多 Library；
- 快速清点、可恢复信息补全和 Inbox；
- Inbox 决定进入便携层：接受、人工归类、忽略、已处理与撤销；
- 可编辑 Group：新建、保存派生混合组、增删成员、调整顺序、设封面、改名与删除关系；
- 系列批量编辑：重命名、批量增删成员、调整顺序、清空编号与删除系列，一次原子写入；
- 图片、目录 ImageSet、ZIP/CBZ、视频和系统相册导入；
- 阅读/播放进度、搜索、元数据编辑、批量操作；
- 逻辑回收站、永久删除保护、Organizer 可恢复事务；
- Series 书架和顺序字段；
- 混合图片/视频的派生展示；
- 有界本机离线预览；
- Edition 快速/深度比较、重复页报告、虚拟合并与跨来源页计划阅读；
- 本机压缩包中央目录缓存及设置页统计/清除；
- Schema v4 的 Asset/Work/Edition/Group/Series 便携模型及 v3 快照转换。

下一步按顺序：

1. 在用户准备好时，用手机直连真实 E 盘验证全量清点、断点补全和大量视频封面，再用证据决定是否做清点检查点和数据库分页；
2. 完成系列阅读流程：话列表中的进度/继续入口，以及读完一话后可控地进入下一话；
3. 继续阅读与列表交互打磨：漫画页复用统一缩放容器、长列表拖拽自动滚动、密度和返回行为。

## 12. 测试期格式策略

Rem 尚未稳定发布：

- 只维护当前 Schema v4；
- v3 只保留一次性、带备份的转换入口，不在正常读写路径保留多套分支；
- 更早的试验 Schema 不再支持；
- 转换失败时 `library.json` 不得提前升级；
- 转换永远不能修改媒体原文件；
- 未知更高版本始终拒绝写入。

稳定发布后，才为已经发布的版本建立正式兼容承诺。

## 13. 完成标准

一项功能只有同时满足以下条件才算完成：

- 领域语义和便携真相明确；
- 不依赖不可恢复的本机状态；
- 失败和中断不会损坏媒体；
- 有单元测试；涉及 SAF 时有 DocumentsProvider 或真机测试；
- 用户指南、架构、变更记录和开发日志按各自职责更新；
- 明确区分已验证结果与尚待真实 Library 验证的推断。

# 更新记录

这里只记录面向使用者的版本变化。开发过程中的问题定位、判断依据与解决措施见 `docs/DEV_LOG.md`。

## 未发布

### 系列连续阅读

- 进入系列不再直接看到作品网格，而是先看到**话列表**：每话显示序号、编号（季/集/卷/章）与阅读状态（未开始 / 第 N / M 页 / 已读完），顶部有整体系列进度。
- 修复接受扫描建议后话列表“不认识”这个系列的问题：此前只有重新扫描才会建立系列信息，导致标题显示成“N 个未归系列作品”、“编辑系列”入口消失、每话编号标签也没有了。
- 修复只打开一页的章节被立刻标记为已读的问题：现在打开后停在“未开始”，滚到话末才记为已读。
- 顶部提供**继续阅读**入口：优先回到最近打开且未读完的那一话，否则从第一话未读处开始，全部读完则从第一话重新开始；第 1 页也正确显示为已经开始。
- 在阅读器里读到一话末尾会出现“本话已读完 / 阅读下一话”，可以直接进入下一话；也可以开启“读完一话后自动进入下一话”（默认开启，仅本机偏好，可随时关闭）。
- 作品详情页也会显示「下一话」，从系列打开的作品才能一路往下读；从搜索等入口单独打开一篇作品时不会突然变成整系列阅读。
- 可见标题不再带上目录名里的下载序号前缀（例如 `05 第5话 目录版` 显示为第 5 话的 `目录版`）。
- 修复恢复到最后一页就误判读完/自动跳话、旋转后跳页、完成后重新阅读仍回到末页，以及末话“完成阅读”无有效去向的问题。
- 修复“重新打开已读完的章节会被再次判为读完”的问题：现在只有向前读到物理末尾（可自动进入下一话）或点击话末入口，才会把一话记为已读；只是打开或滑动一屏看完不再算完成。
- 同名但 ID 不同的 Series 不再被书架或本机投影合并；系列身份始终使用稳定 ID。
- 修复第 1 页被显示成“未开始”：真正打开过的话会显示“第 1 / N 页”，重新打开已读完的话从第 1 页开始。
- 修复话列表顺序跟随作品列表排序的问题：刚加入的新话现在按系列自身的阅读顺序排到正确位置，「下一话」也随之正确。

### 漫画阅读器统一缩放

- 漫画页改用与单图查看器相同的缩放容器：双击在“适应宽度 ↔ 2.5×”之间切换，双指围绕手指缩放且跟随手指位置，放大后不会被拖出可见范围，缩小回静止状态会重新对齐页首。
- 不再出现“看着能滑却滑不动”的空白拖动：放大后在当前可见页范围内的平移被精确钳制，未放大时上下滑动仍然是连续翻页。
- 缩放几何新增“按宽度铺满”摆放方式与可见切片钳制，并新增对应单元测试与设备手势测试。

### 单图查看器缩放与黑底

- 单图查看器：沉浸黑底、双指围绕手指缩放、单指平移、双击在适应屏幕与 2.5× 之间切换；适应屏幕时不能平移，放大后不会被拖出屏幕边界，未放大时滑动仍交给外层翻页。
- 缩放几何（钳制、围绕焦点缩放、双击目标）是纯逻辑并有单元测试；真实手势（双指、双击、拖动）由设备上的 Compose 手势测试验证。漫画页现在复用同一套几何与容器。

### 长列表拖动

- 分组与系列编辑器里长按拖动成员时，手指靠近列表上/下边缘会**自动滚动**，因此数百个成员也能一次拖到目标位置；拖动中的行会跟随滚动，不会掉队。
- 修复长距离自动滚动时“行在动、顺序没变”的问题：列表实际滚过的距离现在真正参与成员重排，手指停住不动也能连续跨越多行；同时手指位置不会再被自动滚动带偏，边缘滚动速度保持稳定。
- 自动滚动速度按列表高度的 25%（上限 160 px）在边缘处渐变，列表中间保持静止，避免误滚。

### 便携写入与扫描一致性

- 便携文档替换现在把旧版本放在固定的恢复路径；如果 App 在“旧版本已移开、新版本尚未就位”之间结束，下次读取会恢复旧版，不再把元数据或 Library 身份误判为缺失。
- 同一 App 内的进度、回收站、元数据、Group、Series 和 Inbox 写入统一串行化，避免两个“先读后改再整份写回”的操作互相覆盖。
- 接受/人工归类 Inbox 时先写 Work 元数据，成功后才写决定；目录写入失败不会把仍未保存的分类项藏出 Inbox。
- 长时间清点不再持有一份过时的便携元数据快照；目录遍历完成后会重新读取最新 catalog/state/Inbox，再更新本机索引。
- **信息补全不再覆盖同时进行的编辑**：补全读取媒体内容时不持有便携写锁，提交前重新读取当前行并按字段合并（人工字段始终优先）。此前若在补全过程中修改同一作品，本机索引会暂时回退到修改前的值。
- 保存元数据同样按字段合并：编辑器只写入真正改动的字段，不会把补全刚写入的哈希、页数或拍摄时间覆盖回旧值。
- 修复陈旧编辑器保存时丢失补全期间新增的人工字段来源；本机进度/回收站投影现在会清除便携文档中已不存在的旧状态，而不是只增量覆盖。
- 目录型图片集不再只在第一次扫描时识别系列与编号：未变化的目录也会重新推导路径信息，因此带标签的话目录（例如 `05 第5话 目录版`）会正确归入所属系列并排在第 5 位。

### 漫画阅读修复

- 修复部分漫画（条目未压缩且带扩展数据描述符的 CBZ）在阅读器里整本打不开、每页显示“此页无法解码”的问题：阅读器此前用的是一个没有压缩包缓存的解码实例，只能走会抛异常的旧路径。现在压缩包缓存成为必需依赖，阅读器、离线预览与页清单共用同一个实例。
- 解码前的流全部按可回退方式包装（`BitmapFactory` 的尺寸探测需要回退流位置），并在无法解码时把具体条目与原因写进诊断日志，便于定位。

### 比较与合并的界面修复

- 修复从“混合图片/视频”入口进入图集详情时，“比较版本”里没有任何可选来源的问题（作品列表没有继续传递到内层页面）。
- 修复“生成虚拟合并版本”按钮被逐页报告挤出可视区域、无法点击的问题：主操作现在固定在对话框按钮行，逐页列表限制高度，二次确认独立成对话框。

### 分组与系列的显示修复

- 修复“保存为 Group 后界面不刷新”的问题：设备索引的投影类型缺少序列化注解，导致保存分组/系列时投影写入在运行时失败（便携元数据其实已经写对）。现在保存后立即出现在“手动分组”里。
- 修复分组页签计数：已保存为 Group 的目录不再同时计入“目录派生”，页签数量与列表一致。
- 修复“新建分组”按钮在已经存在分组或目录派生分组后点击无反应的问题（对话框只在空列表分支里创建）。
- 修复 Inbox 多选在旋转或切换页面后卡住的问题：现在会保留选择，并可用“完成”退出多选。

### 回收站与整理

- 修正回收站保留天数的说明：到期项目会在下次启动 Rem 时直接真删除并提示数量，不会再次询问；填 0 或选“永久”则只能手动删除。此前的说明声称还需身份验证，与实现不符。
- 修复在整理页预览计划后切换 Library 仍显示并可执行上一个 Library 计划的问题。

### 压缩包读取修复

- 修复部分 CBZ 无法读取的问题：条目以“未压缩 + 扩展数据描述符”存放的压缩包会被流式读取器拒绝，导致页数显示为 0 或 1，且无法阅读、无法参与版本比较。现在压缩包先复制到应用缓存再用 `ZipFile` 走中央目录读取，这类文件可以正常统计页数、阅读页清单与 ComicInfo。
- 顺带解决“每翻一页重读整个压缩包”的性能问题：缓存副本支持随机访问，连续翻页不再重复读取整个文件。缓存位于系统可回收的应用缓存目录，默认上限 512 MiB，超出后按最久未使用淘汰，不影响 Library 内任何文件。
- “设置 → 压缩包阅读缓存”现在显示副本数量和占用空间，并可一键清除；清除只影响 App 私有缓存。
- 正在打开的压缩包不会被自己触发的容量清理删掉；单个超过 512 MiB 的 CBZ 可以暂时超额缓存并正常阅读。

### 交互打磨

- 卡片长按面板新增「加入分组…」「加入系列…」与「比较版本…」；加入时若作品已在分组中会提示，若已属于另一个系列则会跳过并报告数量，不会擅自把作品从原系列移走。
- “图片 / 视频”与“漫画 / 阅读”的列表新增多选工具条：一次把选中的作品加入分组或系列、批量收藏、批量追加作者/Tag/Collection、批量移入回收站。
- 批量元数据对话框改为全局共用（相册与图书馆列表行为一致）；多选状态下按返回键退出多选而不是离开页面。
- 版本比较对话框现在也能从卡片直接打开。

### Edition 比较与虚拟合并（数据层）

- 新增按页比较与虚拟合并的底层能力：快速比较完全不读取内容（只用页数与条目大小），深度比较按页读取一次并计算内容哈希；压缩包只顺序读取一遍，重复比较复用本机缓存。
- 比较报告区分“完全重复 / 同名但内容不同 / 疑似重复（未读内容）”，绝不把未读内容的页称为重复。
- 虚拟合并写入同一作品的新的“版本”（页计划跨来源），不复制、不移动、不删除任何媒体；两个来源各自保留原有版本。来源、深度与报告计数写入 `.gallery/imports/`。
- 图片集作品详情页新增“比较版本”：先选另一个来源，再选择快速比较（不读内容）或深度比较（每个来源读一遍，可取消）；报告区分完全重复 / 同名但内容不同 / 疑似重复 / 仅一侧有，并显示实际读取量与耗时。
- 比较结果可以一键“生成虚拟合并版本”：在作品上新增一个按合并顺序排列的版本并设为默认，阅读器按该页计划连续读页，页可以来自不同目录或压缩包；原有版本与媒体文件保持不变，来源证据写入 `.gallery/imports/`。

### 系列编辑

- “漫画 / 阅读 → 系列书架”进入某个系列后新增“编辑系列”：批量添加成员（可按标题、路径或作者搜索多选）、移出成员、用上下箭头或“移动到指定序号”调整阅读顺序、重命名系列、一次性清空季/集/卷/章编号、删除系列。
- 重命名与调整顺序不会丢失已有编号；清空编号是独立选择，会先提示当前状态。
- 保存时只写入系列关系与编号，并把改动过的作品标记为人工系列决定，之后的扫描不会再用目录名把它们重新归类（包括刚被移出系列的作品）。
- 删除系列只删除关系与编号；作品、版本和媒体原文件都保持不变。
- 本机索引升级到 v9，新增可丢弃的 `series` 投影表；接入或扫描时用便携目录文档重建。
- 分组与系列编辑器都支持长按行首手柄拖拽排序；拖拽、上下箭头与“移动到指定序号”三种方式等价，都只在点“保存”时才写入 Library。

### 可编辑分组

- “图片 / 视频 → 分组”现在分「手动分组」和「目录派生」两类：派生分组只是扫描推断的浏览入口，保存为 Group 后才是可编辑的用户决定。
- 分组可以新建、重命名、添加成员（可从全库搜索多选）、移出成员、用上下箭头调整顺序、指定封面成员，也可以删除分组关系。
- 同一目录重复“保存为 Group”只会更新同一个分组；分组使用由主作品推导的稳定 ID。
- 成员、顺序和封面改完点“保存”才写入 Library；分组是便携数据，重建本机索引或换手机后仍然存在。
- 删除分组、移出成员只删除“一起浏览”的关系，不会移动、重命名或删除任何媒体。
- 本机索引升级到 v8，新增可丢弃的 `groups` 投影表；接入或扫描时会用便携目录文档重建该投影。

### Inbox 决定进入便携层

- Inbox 现在分「媒体建议 / 其他待判断 / 已忽略 / 已处理」四个分区；待判断路径可以标记“已处理”或“忽略”，被忽略的媒体和路径仍留在原位置，只是不再出现在普通视图。
- 忽略、已处理、接受和人工归类都会写入 Library 的 `.gallery/state/inbox.json`。删除本机索引、换手机或重新接入同一目录后，被忽略的内容不会自己回到 Inbox。
- “已忽略 / 已处理”都可以撤销；撤销只删除决定，不修改作品元数据，也不移动或删除任何媒体。
- 作品重命名或整理移动后，决定会跟着 `work_id` 继续生效；永久删除作品时会一并清理它的决定。
- Library 根目录的 `GALLERY_LIBRARY.md` 与 `.gallery/schema/v4.json` 已说明该文档，Agent 写决定时必须标记 `agent:<标识>` 并保留已有决定。
- 本机索引升级到 v7：新增 `media.inbox_disposition` 与 `discoveries.disposition` 两个可丢弃的镜像列，重扫不会清除已决定路径的状态。

### 便携格式

- 便携格式升级到 Schema v4：物理来源 `Asset`、逻辑作品 `Work`、取得版本 `Edition`、浏览集合 `Group` 和顺序集合 `Series` 分开保存，关系不再内嵌在单个媒体条目中。
- 接入测试期 Schema v3 Library 时，会先备份身份、目录、状态、说明和旧 Schema，再转换为 v4；转换完成前不会提前更新 Library 身份。更早的试验格式不再保留兼容分支。
- Library 根目录生成的 `GALLERY_LIBRARY.md` 已同步为 v4 Agent 整理规则，明确人工字段保护、关系职责、来源标签和物理文件安全边界。
- 仓库说明文件重新按“产品规范、Agent 规则、当前架构、用户操作、历史记录、版本变化”分工，移除重复阶段计划和已失效描述。

### 可靠性修复

- 修复 SAF 新建文件或目录后错误覆盖根目录缓存的问题；首次接入、新建分类目录和便携元数据原子写入不再因路径错位失败。
- 修复多层目录解析把子路径误当父路径的问题；已经存在的 `.gallery/schema` 等目录不再被误建成带 ` (1)` 的副本，文件提供方调整名称后上层也会使用实际路径。
- Library 初始化改用根目录独占临时租约；并发接入不会生成第二份身份，进程异常留下的过期租约会在 15 分钟后自动恢复。
- 文件提供方暂时无法读取某个目录时，扫描会保留该子树已有索引，不再把其中所有项目误标为需要修复。
- 完整脱敏异常摘要中的 URI 与主机路径；导出操作会等待已排队日志落盘，并在主线程打开系统分享界面。

### 性能与交互

- 漫画 / 动漫新增“系列书架 / 全部作品”两种展示；进入系列后只在该系列顺序中浏览，单篇和未归系列作品仍保留独立入口。
- 系列编辑补齐可选的手动顺序、季、集、卷和章；手动顺序优先，未设置时按季/集或卷/章排列，所有编号都可留空。
- 修复编辑作品时丢失已有季、集、卷、章的问题；同名系列会复用稳定标识，旧数据中同名但 ID 不同的条目也会合并展示。
- 扫描到的新内容现在只进入 Inbox，不再在用户确认前混入相册、图片 / 视频、漫画 / 动漫和全局搜索。
- Inbox 支持单项或多选接受当前识别建议；打开编辑器修改过的字段标记为 `manual`，直接接受的建议保留 `filename` / `comic_info` 自动来源，后续仍可由 Agent 安全更新。
- 离线识别补充 `eh/` 下载根目录、`JM/数字.zip` 和“系列目录/编号标题.cbz”结构；这些结果仍然只是 Inbox 建议。
- Inbox 新增“其他待判断”：不支持的文件格式与结构不明确的目录不再静默消失；记录只保存在可重建的本机索引中，不改动原文件，也不会把未知内容强行当成媒体。
- 扫描会排除 `.gallery/`、`.nomedia`、`.ehviewer`、`ComicInfo.xml` 和常见系统缩略图等内部文件或 sidecar，避免 Inbox 被已知噪声淹没。
- 本机索引升级到 v5 时采用增量建表，保留既有 Library、媒体与阅读进度。
- ZIP/CBZ 未变化时复用页数、指纹和既有元数据；变化时一次遍历同时统计页面并读取 ComicInfo。
- 目录漫画指纹加入修改时间，同名同大小但内容已替换的页面不再沿用旧指纹。
- 大型 Library 扫描会持续显示已读取目录数、条目数和已识别项目数。
- 扫描提交缺失文件状态时只读取 ID 与相对路径，不再把整张媒体表和元数据 JSON 重复载入一次。
- 大型 Library 首次扫描拆成“快速清点”和“媒体信息补全”：先提交可浏览索引，再分批读取哈希、ZIP/CBZ、ComicInfo 与拍摄信息。
- 媒体信息补全进度保存在本机数据库 v6；应用被结束后再次打开可从剩余项目继续，已经提交的批次不再重读。补全队列始终只保存在本机，不进入便携元数据。
- “图片 / 视频”新增混合分组：同一叶子目录中的图片集和直属视频只显示一个入口，进入后可浏览整组图片并逐个播放附带视频；独立视频页不再重复显示这些组内视频。
- 含图片和视频的目录默认进入“图片 / 视频”；位于 `Comics/`、`ImageSets/`、`Works/`、`Manga/` 等明确作品根目录时仍归入作品库，人工修改过的归属始终优先。
- 浏览媒体卡片时会按需保存最长边 512 px 的本机离线小预览；移动 Library 拔出后仍可辨认内容。缓存限制为 256 MiB / 20,000 张，可在设置中查看和清除，不写入移动介质，也不替代原图。

## Rem 0.0.3 — 2026-09-16

针对"接入大容量可移动 Library 时长时间加载不出来"的修复版本，并首次提供可导出的诊断日志。

### 主要能力

- 新增诊断日志：写入本机 App 私有目录，按天滚动、保留 14 天；崩溃自动落盘；`设置 → 诊断日志` 可查看、导出或清除。导出走系统分享，日志不写入 Library。
- 新增 `dist/Rem-<版本>-mapping.txt`：每次 `assembleRelease` 自动归档 R8 mapping，用于还原真机崩溃堆栈。

### 性能修复

- SAF 目录列举改为单次投影查询，一次取回名称、类型、大小和修改时间，替代 `DocumentFile` 每个子项 5 次的属性查询。
- 新增路径→文档与目录列表缓存，同一路径不再从 Library 根目录重复解析；重命名和删除只失效受影响的子树，批量整理不再退化为全量重走。
- 扫描改为增量：大小和修改时间都未变化的文件复用已记录的指纹与拍摄信息，不再重复读取内容。
- 扫描结果改为单事务批量写入本机索引，不再逐条提交。

### 缺陷修复

- 修复重复接入同一目录会在 `.gallery` 中生成两份 Library 身份的问题：初始化改为先原子占位，接入串行化，已登记的目录复用磁盘上的身份。
- 修复写入被文件提供方改名成 ` (1)` 副本时仍被视为成功的问题：提交会校验实际落点，冲突时回滚旧版本并报错，不再留下重复文件。
- 修复重复点击扫描会并发执行两次全量扫描的问题。

### 发布说明

- 本机 SQLite 索引和缩略图只是缓存；升级后可直接在 `设置 → 诊断日志` 查看记录。
- `.gallery/`、Kotlin 包名与 Gallery Schema 标识保持不变，旧 Library 无需迁移。

## Rem 0.0.2 — 2026-09-16

首个以 Rem 名称发布的 Android 预览版本。

### 主要能力

- 接入一个或多个实体文件夹作为可移动 Library，媒体保持在用户选择的位置。
- 以“相册”“图片 / 视频”“漫画 / 动漫”三个主入口区分时间线、目录分类和作品管理。
- 支持目录漫画、ZIP/CBZ、普通图片、GIF、Animated WebP、SVG、超大图片和本地视频。
- 漫画提供作品详情、连续阅读、进度、页操作、缓存与方向感知预加载。
- 识别 JM、EhViewer 和 Pixiv 常见下载命名，并允许 Agent 按规则辅助补全元数据。
- 作者、标签、Series、搜索、逻辑回收站、安全文件整理和便携元数据均已形成完整本地链路。

### 发布说明

- 用户可见名称和启动图标更新为 Rem，正式应用 ID 使用 `com.susnowy.rem`；它会与早期开发版并存，重新接入原 Library 后可恢复 `.gallery` 中的便携状态。
- 当前不内置站点抓取、账号 Cookie 或云同步。
- 自动识别和 Agent 同步不得覆盖标记为 `manual` 的人工字段。
- 正式 APK 使用仓库外的长期 JKS 签名；密钥及密码不得写入仓库，后续更新必须继续使用同一密钥。

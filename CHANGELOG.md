# 更新记录

这里只记录面向使用者的版本变化。开发过程中的问题定位、判断依据与解决措施见 `docs/DEV_LOG.md`。

## 未发布

### 可靠性修复

- 修复 SAF 新建文件或目录后错误覆盖根目录缓存的问题；首次接入、新建分类目录和便携元数据原子写入不再因路径错位失败。
- 修复多层目录解析把子路径误当父路径的问题；已经存在的 `.gallery/schema` 等目录不再被误建成带 ` (1)` 的副本，文件提供方调整名称后上层也会使用实际路径。
- Library 初始化改用根目录独占临时租约；并发接入不会生成第二份身份，进程异常留下的过期租约会在 15 分钟后自动恢复。
- 文件提供方暂时无法读取某个目录时，扫描会保留该子树已有索引，不再把其中所有项目误标为需要修复。
- 完整脱敏异常摘要中的 URI 与主机路径；导出操作会等待已排队日志落盘，并在主线程打开系统分享界面。

### 性能与交互

- 漫画 / 动漫新增“系列书架 / 全部作品”两种展示；进入系列后只在该系列顺序中浏览，单篇和未归系列作品仍保留独立入口。
- 系列编辑补齐可选的手动顺序、季、集、卷和章；手动顺序优先，未设置时按季/集或卷/章排列，所有编号都可留空。
- 修复编辑作品时丢失已有季、集、卷、章的问题；同名系列会复用稳定标识，旧数据中同名但 ID 不同的条目也会合并展示。
- 扫描到的新内容现在只进入 Inbox，不再在用户确认前混入相册、图片 / 视频、漫画 / 动漫和全局搜索。
- Inbox 支持单项或多选接受当前识别建议；打开编辑器修改过的字段标记为 `manual`，直接接受的建议保留 `filename` / `comic_info` 自动来源，后续仍可由 Agent 安全更新。
- 离线识别补充 `eh/` 下载根目录、`JM/数字.zip` 和“系列目录/编号标题.cbz”结构；这些结果仍然只是 Inbox 建议。
- Inbox 新增“其他待判断”：不支持的文件格式与结构不明确的目录不再静默消失；记录只保存在可重建的本机索引中，不改动原文件，也不会把未知内容强行当成媒体。
- 扫描会排除 `.gallery/`、`.nomedia`、`.ehviewer`、`ComicInfo.xml` 和常见系统缩略图等内部文件或 sidecar，避免 Inbox 被已知噪声淹没。
- 本机索引升级到 v5 时采用增量建表，保留既有 Library、媒体与阅读进度。
- ZIP/CBZ 未变化时复用页数、指纹和既有元数据；变化时一次遍历同时统计页面并读取 ComicInfo。
- 目录漫画指纹加入修改时间，同名同大小但内容已替换的页面不再沿用旧指纹。
- 大型 Library 扫描会持续显示已读取目录数、条目数和已识别项目数。
- 扫描提交缺失文件状态时只读取 ID 与相对路径，不再把整张媒体表和元数据 JSON 重复载入一次。
- 大型 Library 首次扫描拆成“快速清点”和“媒体信息补全”：先提交可浏览索引，再分批读取哈希、ZIP/CBZ、ComicInfo 与拍摄信息。
- 媒体信息补全进度保存在本机数据库 v6；应用被结束后再次打开可从剩余项目继续，已经提交的批次不再重读。便携 `.gallery` Schema 保持不变。
- “图片 / 视频”新增混合分组：同一叶子目录中的图片集和直属视频只显示一个入口，进入后可浏览整组图片并逐个播放附带视频；独立视频页不再重复显示这些组内视频。
- 含图片和视频的目录默认进入“图片 / 视频”；位于 `Comics/`、`ImageSets/`、`Works/`、`Manga/` 等明确作品根目录时仍归入作品库，人工修改过的归属始终优先。

## Rem 0.0.3 — 2026-09-16

针对"接入大容量可移动 Library 时长时间加载不出来"的修复版本，并首次提供可导出的诊断日志。

### 主要能力

- 新增诊断日志：写入本机 App 私有目录，按天滚动、保留 14 天；崩溃自动落盘；`设置 → 诊断日志` 可查看、导出或清除。导出走系统分享，日志不写入 Library。
- 新增 `dist/Rem-<版本>-mapping.txt`：每次 `assembleRelease` 自动归档 R8 mapping，用于还原真机崩溃堆栈。

### 性能修复

- SAF 目录列举改为单次投影查询，一次取回名称、类型、大小和修改时间，替代 `DocumentFile` 每个子项 5 次的属性查询。
- 新增路径→文档与目录列表缓存，同一路径不再从 Library 根目录重复解析；重命名和删除只失效受影响的子树，批量整理不再退化为全量重走。
- 扫描改为增量：大小和修改时间都未变化的文件复用已记录的指纹与拍摄信息，不再重复读取内容。
- 扫描结果改为单事务批量写入本机索引，不再逐条提交。

### 缺陷修复

- 修复重复接入同一目录会在 `.gallery` 中生成两份 Library 身份的问题：初始化改为先原子占位，接入串行化，已登记的目录复用磁盘上的身份。
- 修复写入被文件提供方改名成 ` (1)` 副本时仍被视为成功的问题：提交会校验实际落点，冲突时回滚旧版本并报错，不再留下重复文件。
- 修复重复点击扫描会并发执行两次全量扫描的问题。

### 发布说明

- 本机 SQLite 索引和缩略图只是缓存；升级后可直接在 `设置 → 诊断日志` 查看记录。
- `.gallery/`、Kotlin 包名与 Gallery Schema 标识保持不变，旧 Library 无需迁移。

## Rem 0.0.2 — 2026-09-16

首个以 Rem 名称发布的 Android 预览版本。

### 主要能力

- 接入一个或多个实体文件夹作为可移动 Library，媒体保持在用户选择的位置。
- 以“相册”“图片 / 视频”“漫画 / 动漫”三个主入口区分时间线、目录分类和作品管理。
- 支持目录漫画、ZIP/CBZ、普通图片、GIF、Animated WebP、SVG、超大图片和本地视频。
- 漫画提供作品详情、连续阅读、进度、页操作、缓存与方向感知预加载。
- 识别 JM、EhViewer 和 Pixiv 常见下载命名，并允许 Agent 按规则辅助补全元数据。
- 作者、标签、Series、搜索、逻辑回收站、安全文件整理和便携元数据均已形成完整本地链路。

### 发布说明

- 用户可见名称和启动图标更新为 Rem，正式应用 ID 使用 `com.susnowy.rem`；它会与早期开发版并存，重新接入原 Library 后可恢复 `.gallery` 中的便携状态。
- 当前不内置站点抓取、账号 Cookie 或云同步。
- 自动识别和 Agent 同步不得覆盖标记为 `manual` 的人工字段。
- 正式 APK 使用仓库外的长期 JKS 签名；密钥及密码不得写入仓库，后续更新必须继续使用同一密钥。

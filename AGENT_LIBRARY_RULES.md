# Rem Library 文件模式规范

规则版本：1

本文件描述一个 Rem Library 的目录结构和文件命名规范，供 AI 整理底层文件时作为唯一依据。
Rem 在每次接入 Library 时都会用仓库中的最新版本覆写本文件，所以这里的内容始终是当前规则。

整理动作由人执行，Rem 只负责读取和展示。Rem 不会改名、移动、删除或覆盖任何媒体文件。

## 1. 顶层目录

Library 根目录下只允许这四个条目，不得增加其他业务目录，也不得在根目录直接放媒体文件：

```
Library/
  相册/
  画集/
  待分类/
  .gallery/
```

| 目录 | 用途 |
| --- | --- |
| `相册/` | 可浏览。直接存放图片和视频，不建子文件夹。 |
| `画集/` | 可浏览。第一层是 `作者名称-项目名称/` 项目文件夹。 |
| `待分类/` | 只用于暂存。Rem 不读取、不索引、不展示、不整理。 |
| `.gallery/` | Rem 的内部状态，可随时重建。不要把媒体放进来。 |

## 2. 相册文件模式

```
相册/
  任意文件名.jpg
  任意文件名.mp4
```

- 媒体文件直接放在 `相册/` 下，不建立子文件夹。
- 文件名不设强制格式，保持原始文件名即可。
- 支持 `jpg` `jpeg` `png` `webp` `gif` `bmp` `avif` `heic` `heif` 和
  `mp4` `m4v` `mov` `mkv` `webm` `avi` `3gp` `ts` `wmv` `flv`。
- 排序时间依次取：图片 EXIF 拍摄时间或视频自身拍摄时间 → 文件修改时间。
- 地点只读取媒体已有的 GPS 或地点元数据并显示为坐标；没有证据就不显示。
  不根据文件名、目录名或网络信息推断地点。

## 3. 画集文件模式

```
画集/
  作者名称-项目名称/
    0001.jpg
    0002.png
    0003.mp4
```

- 第一层只允许项目文件夹，文件夹名必须是 `作者名称-项目名称`。
  作者名称和项目名称都必填，分隔符是第一个半角连字符 `-`，两侧要有实际文字。
- 项目文件夹内直接放媒体文件，**禁止继续建立子文件夹**。
- 规范文件名为四位数字序号加原扩展名，从 `0001` 开始，最大 `9999`。
- 未满四位不补零的写法（如 `1.jpg`）、非数字开头、或没有扩展名的文件都算不规范。
- 发现缺号时，后续规范文件可以自动向后顺延；不得覆盖已有文件。
- 不符合规范的其他文件放在规范序号文件之后，排序方式不作规定。

## 4. 连字符消歧

文件夹名按**第一个**半角连字符切分，因此：

| 文件夹名 | 作者名称 | 项目名称 |
| --- | --- | --- |
| `山田-夏日` | 山田 | 夏日 |
| `A-B-C` | A | B-C |
| `无连字符` | 不合规，会被报告 | 不合规，会被报告 |

## 5. Rem 的读取范围

- 只在用户显式接入或刷新时扫描，不做后台全盘扫描。
- 只读取 `相册/` 直属文件和 `画集/*/` 直属文件，不递归进入更深层级。
- 不读取 `待分类/`。
- 不读取任何媒体内容以外的用途：图片只读 EXIF，视频只读容器元数据，不修改它们。

## 6. 不规范内容的处理

Rem 发现不规范内容时只报告，不自动修正。整理需要由人（或按本规范操作的 AI）完成。
会被报告的情况包括：

- 项目文件夹名不是 `作者名称-项目名称`。
- 项目文件夹内出现子文件夹。
- 项目文件夹内的媒体文件不是四位数字序号。
- `相册/` 或 `画集/` 下出现不支持的扩展名。

## 7. .gallery 内部

```
.gallery/
  RULES.md      本文件。每次接入 Library 都会被覆写为最新规则。
  index.json    Rem 的扫描缓存，可删除，下次接入会重建。整理时不需要改动它。
```

## 8. 系统相册隐藏控制标记

Rem 可在 Library 根目录写入或移除 `.nomedia`，用于控制后续系统媒体扫描：

- 开启隐藏：创建 `Library/.nomedia`；不修改或删除任何媒体。
- 关闭隐藏：移除 `Library/.nomedia`；不恢复或清除已有系统媒体库记录。
- `.nomedia` 是唯一允许 Rem 写入 Library 业务目录的控制文件；`相册/`、`画集/` 内媒体仍保持只读。
- 已经进入 `MediaStore` 或系统相册的媒体是否立即消失，取决于系统重新扫描，Rem 不强制删除媒体库记录。

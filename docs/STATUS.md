# Rem 当前状态

## 规范基线

2026-09-20 已根据用户确认重置产品规范。详细规则见 [AGENT_PRODUCT_RULES.md](../AGENT_PRODUCT_RULES.md)，Library 文件模式见 [AGENT_LIBRARY_RULES.md](../AGENT_LIBRARY_RULES.md)。

同日，内置查看器作为新的明确要求重新确认并移出暂停清单，规则见 [AGENT_PRODUCT_RULES.md](../AGENT_PRODUCT_RULES.md) 第 5.1 节。**该能力已于同日晚实现并接入点击入口**，实现要点与真机实测见下文。

同日稍晚，规范再次调整：**画集支持多级目录**、画集提供**可选排序**（序号/名称/拍摄时间/修改时间/大小）、**扫描按文件夹增量落盘并可中断续扫**、界面显示扫描进度。**这些能力已实现并在模拟器上验证**，见下文「画集多级目录与扫描」一节。

缩略图缓存**留在设备本机的应用私有目录**：Android 11+ 的分区存储不允许应用用文件 API 写入 Library 内的目录（实测 `EPERM`），因此"缩略图随库走、换设备免重建"这一设想在当前权限模型下不可行，除非申请 `MANAGE_EXTERNAL_STORAGE` 全盘访问（代价是应用获得所有文件的访问权）。现状是换设备后缩略图重新生成。

## 当前实现

应用已从零实现最小 Library 接入和浏览流程：

| 模块 | 位置 | 当前职责 |
| --- | --- | --- |
| 模型 | `model/Entry.kt`、`model/Index.kt` | 目录条目、媒体类型和索引序列化 |
| 存储 | `storage/LibraryTree.kt`、`storage/LibraryStore.kt` | SAF 目录读取、`.gallery/index.json` 缓存和 `RULES.md` 写入 |
| 媒体 | `media/MediaProbe.kt` | 读取图片 EXIF 和视频容器元数据 |
| 扫描 | `scan/Scanner.kt` | 读取 `相册/` 直属文件与 `画集/` 的整棵树（层级不限）；文件夹内并发读元数据，边扫边落盘、可中断续扫 |
| 查看器 | `ui/Viewer.kt`、`ui/ViewerState.kt` | 内置查看与播放：连续翻页、图片缩放、视频播放与失败提示 |
| 界面 | `ui/` | 相册网格、画集项目列表、项目详情、右侧滑块和系统查看器/播放器调用 |

接入后先展示缓存，再进行后台增量扫描；扫描只读取 `size` 或修改时间变化的文件。应用不修改任何媒体文件。

## 画集多级目录与扫描（已实现）

2026-09-20 晚确认并实现的调整，逐项对应：

| 规范要求 | 实现位置 | 验证 |
| --- | --- | --- |
| `画集/` 支持多级目录，层级不限 | `scan/Scanner.kt` 用显式栈深度优先遍历，只读目录、不跟随符号链接式深挖 | 模拟器合成库：`画集/Alice-夏日合集/花絮/原图/raw_001.png` 等两层嵌套文件全部入索引 |
| 命名自由，违规只报目录结构 | `Scanner.readFiles` 只报不支持的扩展名；第一层命名与 `相册/` 子文件夹仍报 | 3 条违规：`相册/不该有的子文件夹`、`画集/loose-file.jpg`、`画集/Bob-测试项目/notes.txt` |
| 画集可选排序并记住选择 | `model/SortMode` + `UiState.mediaOrder()`，选择存本机 `rem.library` 的 `sort_mode` | 模拟器点选后 `sort_mode=SIZE` 写入偏好；名称序实测 `1, 2, 10` |
| 同层不混排 | `UiState.folderRows` / `folderMedia`：该层有子文件夹就只显示文件夹 | 行文案 `2 个文件夹 · 45 个媒体文件 · 两者都有，先显示文件夹` |
| 扫描增量落盘、可中断续扫 | `ScanSink` 每 400 ms 或每个文件夹结束时上报；`RemViewModel` 在回调里写 `index.json` | 模拟器一次全新扫描上报 3 次（已读 1 → 205 → 312），索引在扫描结束前即已落盘 |
| 扫描进度可见 | `UiState.scanProgress` + `App.kt` 的 `ScanningBar`，显示 `正在扫描 N / M` | 上报序列见上；`M` 在目录列举后由 200 增长到 312 |
| 文件夹内计数含下层 | `UiState.subtreeCounts`，每个状态只算一次 | 顶层项目行显示 45 个媒体文件（含两层子文件夹里的 25 个） |

**元数据读取并发**：文件夹内 6 路并发（`Dispatchers.IO.limitedParallelism(6)`）。模拟器 316 文件的合成库全新扫描 **938–2097 ms**；真机（S 盘、未知规模）此前为小时级，但**并发后的真机数字尚未测得**——真机不在手边，这一条是未验证。

**缩略图**：仍在本机 `cacheDir/thumbnails`，磁盘缓存不设实际上限（Coil 上限设为 512 GB，等于不限）。曾尝试写入 `.gallery/thumbs/`，被 Android 11+ 分区存储以 `EPERM` 拒绝，详见上文说明。

## 真实可用性排查（2026-09-20 晚，进行中）

把合成库放大到 5562 文件（304 个项目、2201 个相册文件、两层嵌套、空文件夹、刻意违规项）后逐项排查。修掉的缺陷：

| 编号 | 问题 | 影响 |
| --- | --- | --- |
| A1 | 第一层项目文件夹从未写入索引（遍历排除了起点） | 索引文件夹数 52 而非 355；界面靠条目路径兜底才没露出来 |
| A2 | 空文件夹在界面上永远不显示 | 项目里唯一的空文件夹消失 |
| A3 | `subtreeCounts` 无限递归（区块根成了自己的子节点） | `StackOverflowError`，实测堆栈确认为自调用 |
| A4 | 条目分组把 `画集` 当成文件夹 | 顶层多出一个叫「画集」的项目行 |
| A5 | 自然序比较器把两个纯文字名称判为相等 | 中文文件夹名排序退化成索引顺序 |
| A6 | provider 缓存过期时建出重复的 `.gallery (1)` | 状态分裂成多份，违反 `.gallery/` 只放两个文件的规范 |

**客观指标（模拟器 RemWSL，x86_64 + KVM）**：

| 指标 | 数值 |
| --- | --- |
| 全量扫描 5562 文件 | 18.4 s（首次含元数据读取）；另一次实测 35.0 s，波动来自模拟器 I/O |
| 增量扫描（全部复用、无文件变化） | 7.4 s |
| 冷启动（进程被杀后，含 5562 条缓存） | 1728 ms |
| 热启动（后台回前台） | 86 ms |
| 索引完整性 | folders 355 = 从条目反推的目录数 355（缺失 0、多余 0） |
| 单测 | 11 个测试类 84 用例 0 失败 |

**仍未验证**：真机（U 盘/SSD）上的扫描与启动耗时；查看器在混合列表里的翻页与视频播放；排序切换在大列表上的手感；缩略图磁盘缓存的命中效果（需先滚动产生缓存）。

## 内置查看器（已实现）

点击媒体在应用内查看或播放，代码在 `ui/Viewer.kt`（界面与播放器）和 `ui/ViewerState.kt`（可单测的纯决策）。`ui/OpenWith.kt` 不再是唯一打开路径，而是始终保留的显式操作与失败回退。

### 内嵌方案

| 用途 | 方案 |
| --- | --- |
| 视频播放 | `androidx.media3:media3-exoplayer:1.11.1` + `media3-ui:1.11.1` |
| 图片查看 | `me.saket.telephoto:zoomable-image-coil3:0.19.0` |
| 连续翻页 | Compose Foundation `HorizontalPager`，`beyondViewportPageCount = 1` |

Telephoto 0.19.0 的传递依赖为 Coil 3.2.0、Kotlin 2.1.21、Compose runtime 1.8.0，与本项目现状（Coil 3.2.0 / Compose BOM 2025.04.01 的 1.8.0 线）一致；Media3 1.11.1 的 Compose 构件 `media3-ui-compose-material3` 在 Google Maven 实测存在，其 `kotlin-stdlib:2.2.10` 在本项目 Kotlin 2.1.20 下编译无警告。

### 实现必须遵守的四条硬规则（均为真机实测得出）

1. **图片请求不得指定原始尺寸**，解码尺寸交给图片库自行决定。见下文探针数据。
2. **视频只能在视频视图真正挂到窗口之后再 prepare**，并在之后留一小段让 surface 就绪。真机上直接点开视频时抢在 surface 之前 prepare，会导致该页永久黑屏、时长显示 0 秒；而滑动进入的视频因为相邻页早已组合、surface 已存在，看起来一切正常——这个差异正是问题的来源。
3. **图片页不得 `stop()` / `clearMediaItems()`**。实测在两次视频之间这样拆解编解码器，会从 `c2.qti.avc.decoder` 抛出 `IllegalStateException: flush() is valid only at Executing states; currently at Released state`，该页随后永久黑屏。切到图片只 `pause()`，下一个视频复用同一编解码器。
4. **不得在任何 effect 中复位「视图已挂载」标记**。实测同一个状态对象在 attach 回调里读到 `true`、30 ms 后又被读成 `false`（effect 因播放器对象被赋值而重启，第一行把标记清掉），结果是直接点开的视频永远不绑定。现在该标记只在视图挂载时置位、真正离开窗口时清位。

另外两条按规范落地的行为：

- `seekable` 必须**按视频复位**。FLV 被拒绝 seek 后会把进度条关掉，不复位就会被下一个视频继承（实测：从 FLV 滑到 MP4 后，MP4 的进度条也不能拖动）。
- 「重试」重建播放器而不是重新 prepare 同一实例：已经死掉的解码器无法复用。

### 探针实测依据

以下数据来自仓库外的独立探针工程（`dev.susnowy.remprobe`，与被测库同版本栈，**不属于本仓库**，因此不能用 `./gradlew :app:testDebugUnitTest` 复现）。设备为真机 `baff50eb`（23127PN0CC，Android 16 / API 36，arm64-v8a）与模拟器 `Rem_API_36`（API 36，x86_64）。所有操作通过界面与 `adb` 完成。

**图片：降采样成立**。Coil 的 `ImageResult.SuccessResult.isSampled` 是直接证据，不需要从尺寸推断。

| 样本 | 源分辨率 | 解码结果 | `isSampled` |
| --- | --- | --- | --- |
| 真实图 `羽毛笔.jpg` | 10800 × 10800（1.166 亿像素） | 4096 × 4096 | true |
| 真实图 `20211023_…_p0.jpg` | 7728 × 4408 | 4096 × 2336 | true |
| 合成图 `img-huge-5000x40000.jpg` | 5000 × 40000（2 亿像素） | 512 × 4096 | true |
| 合成图 `img-strip-2000x20000.png` | 2000 × 20000 | 410 × 4096 | true |

**必须遵守的硬规则：图片请求不得指定原始尺寸。** 在 Pager 中显式使用 `Size.ORIGINAL` 时，6000 × 4000 的图解码失败并记录 `failed to allocate hardware Bitmap!`；去掉尺寸参数后同一张图解出 1200 × 800，失败消失。真机上 26 张图（含上述全部大图）连续翻页零失败，进程未被回收，`app start` 计数为 0，堆稳定在 11–14 MB / 256 MB。

**视频：真机与模拟器逐项一致**。三个 AVI 变体（MPEG-4 SP、Xvid、DivX 3）均可播放并可 seek，与「AVI 可能不可用」的早期猜测相反。

| 格式 | 结果 | 进度拖动 |
| --- | --- | --- |
| MP4 / H.264、HEVC、AV1 | 可播放 | 可拖动 |
| MOV / H.264 | 可播放 | 可拖动 |
| MKV / H.264、HEVC | 可播放 | 可拖动 |
| WebM / VP9 | 可播放 | 可拖动 |
| TS / H.264 | 可播放 | 可拖动 |
| 3GP / H.263 | 可播放 | 可拖动 |
| AVI / MPEG-4 SP、Xvid、DivX 3 | 可播放（DivX 3 无 `videoFormat` 上报） | 可拖动 |
| FLV / H.264 | 可播放 | **不可拖动**，seek 后停在 0s |
| WMV / msmpeg4v2 | **无法播放**，`ERROR_CODE_PARSING_CONTAINER_UNSUPPORTED` | 不适用 |

产品口径（已确认）：不为不常见格式做特殊兼容，FLV 只播不拖，WMV 失败时提示并交给系统应用。

### 真机实现验证（2026-09-20，本轮）

设备仍为小米 `23127PN0CC`（Android 16 / API 36，arm64-v8a）。本轮验证使用的是**临时测试 Library** `/sdcard/任启敬/rem-test`（由本机用 SAF 选择器接入，另有更早的合成 Library 用于模拟器验证），**不是用户的正式 Library**；真实 Library 的验证由用户自行进行。验证样本为该库原有的相册内容（含 3382 万像素大图）与三个本机生成并推入的测试视频（`ver-0099-short.mp4`、`ver-0100-seektest.flv`、`ver-0101-unsupported.wmv`，验证后已从设备删除）。全部操作由用户手动完成，结论取自设备上的 `cache/logs/rem.log` 与界面表现。

| 验证项 | 结果 |
| --- | --- |
| 点图片进入查看器 | 正常显示并可正常打开，缩放、平移、双击、翻页均正常 |
| 直接点视频进入 | 修复后正常：日志为 `绑定第 N 页视频` → `首帧已渲染` → `state=READY duration=…` |
| 滑动进入视频 | 正常出画面 |
| 大图（1.1 亿像素级）翻页 | 未出现解码失败或黑屏（用户实测） |
| FLV | 可播放；拒绝 seek 时隐藏进度条并标注「该格式不支持拖动」，播放继续 |
| WMV | 给出可读提示 `无法解析这个视频容器`，保留重试与「用其他应用打开」 |
| 查看器不写 Library | 打开查看器前后 `.gallery/index.json` 的修改时间不变 |

### 已知缺陷（本轮修复）

| 编号 | 问题 | 修复 |
| --- | --- | --- |
| V1 | 网格点击只有涟漪、不打开查看器 | `MediaGrid` 里 `onOpen = { index }` 是空壳 lambda，语法合法但不调用回调；改回 `onOpen = { onOpen(index) }` |
| V2 | 直接点开视频必然黑屏、时长 0 秒 | 等视频视图挂到窗口并留出 surface 就绪时间后再 prepare（硬规则 2） |
| V3 | 图片页 `stop()`/`clearMediaItems()` 导致 `c2.qti.avc.decoder` 抛 `flush() … Released state`，该页永久黑屏 | 图片页只 `pause()`，不拆编解码器（硬规则 3） |
| V4 | 复位「视图已挂载」标记的 effect 抹掉刚写入的值，直接点开的视频永不绑定 | 不在任何 effect 中复位该标记（硬规则 4） |
| V5 | FLV 的「不可拖动」串到下一个视频 | 每次绑定新视频复位 `seekable` |
| V6 | 直接点开的视频页未显示错误提示仍为黑屏 | 播放器失败改为可见提示 + 重建播放器式重试 |

### 尚未验证

- 后台完全无声：本轮按规范在离开前台时释放播放器（`player.release()`），真机日志可见释放记录；但「按 Home 后确实无声」尚未逐次实测，验收标准仍是「后台完全无声」而不是「已暂停」。
- API 26–28 设备上的行为（AV1 需要 API 29+，HEIC 需要 API 28+，目前只有 API 36 一个验证点）。
- HEIC/HEIF 解码（缺样本，本机无法生成）。
- 真实老资源文件（AVI 样本为合成，DivX 3 不等于真实资源站的 Xvid/DivX 文件）。
- release 构建的体积增量（探针 debug 包为 19.42 MB，不能代表 release；本轮 debug 包为 20.9 MB）。
- 双指缩放的跟手程度由用户在真机上确认「顺畅、能高清查看」，但没有客观指标记录。

## “从系统相册隐藏”实现与验证

设置中的开关只管理 Library 根目录 `.nomedia`：

- 开启时创建 `Library/.nomedia`；关闭时移除该文件；未接入 Library 时不可操作。
- `storage/LibraryTree.setSystemGalleryHidden` 是唯一写入点，返回写入后由 provider 读回的真实状态，界面不显示未经确认的状态。
- 开关决策抽成纯函数 `markerAction` 并配有单测：目标状态与现状一致时不动作，名为 `.nomedia` 的目录按冲突处理、绝不删除。
- ViewModel 串行化该操作，重复点击不会并发写入，因此不会产生 `.nomedia (1)`。
- 不修改、改名、移动、删除或覆盖任何原始媒体；不主动删除 `MediaStore` 已有记录。
- 开关状态由当前 Library 中 `.nomedia` 是否存在推导，不另存本机偏好。

小米官方说明路径任一层存在 `.nomedia` 时，该路径下图片不会被系统媒体服务记录：[KA-09388](https://www.mi.com/global/support/article/KA-09388/)。

### 真机验证（2026-09-20，本轮）

设备小米 `23127PN0CC`（`houji`），Android 16，澎湃 OS `3.0`（`V816`）。经 SAF 文件选择器接入探针 Library `/Download/RemLibraryProbe/`（含 `相册/` 与 `画集/测试作者-测试项目/`），全部操作通过界面点击完成：

| 验证项 | 结果 |
| --- | --- |
| 开→关、关→开连续 4 轮 | 每轮恰好 1 个标记文件，状态与界面一致 |
| 连续 5 次快速点击 | 始终只有 1 个 `.nomedia`，无 `.nomedia (1)` |
| 重启应用 | 开关恢复为开，状态来自 Library 而非本机偏好 |
| 应用外删除 `.nomedia` 后重启 | 开关显示为关，证明状态由磁盘推导 |
| 开启后向两条分支新增媒体并重新扫描 | 磁盘 8 个文件，`MediaStore` 记录 0 条 |
| 关闭后重新扫描 | 8 条记录全部恢复 |

补充结论与边界：

1. 应用创建的 `.nomedia` 是普通文件（本机 provider 写入 44 字节父目录路径）；标记的作用只取决于文件是否存在，空文件行为相同。
2. 本机重新扫描会清除 `.nomedia` 目录下已经建立的 `MediaStore` 记录，移除标记后这些记录会随扫描恢复。此效果强于此前记录，但**仍需系统重新扫描**；应用自身不触发扫描，所以刚切换开关时已有记录可能继续显示，界面提示按此表述。
3. 小米 Gallery 界面本身的即时隐藏/恢复表现未做独立 UI 验证，本轮只验证了底层 `MediaStore` 记录。

验证用探针目录、推送脚本与临时文件已全部从设备删除；`MediaStore` 图片记录数已回到验证前的 77 条基线。

## 模拟器验证（2026-09-20，本轮）

真机拔除后改用 Android 16（API 36）模拟器 `Rem_API_36` 继续验证。测试 Library 由 WSL 用 Pillow/piexif/ffmpeg 生成，共 827 个文件，覆盖各媒体容器、元数据组合、异常命名和 600 项长列表，经 SAF 选择器接入。

### 已确认可用

| 项目 | 结果 |
| --- | --- |
| SAF 接入规模 | 827 个文件；首次全量扫描 16.4s，丢弃缓存后重扫 6.7s，增量重扫 640–750ms |
| `待分类/` 边界 | 根目录可见但不被扫描，827 条条目中不含其内容 |
| EXIF 拍摄时间 | `DateTimeOriginal` 优先，缺失时退到 `DateTime`，再缺失用文件修改时间 |
| GPS 地点 | 规范 EXIF 能读出经纬度；piexif 写出的畸形 GPS IFD 被 ExifInterface 正确拒绝 |
| 目录规则 | 项目列表显示作者/项目与计数；不合规文件名被报告为违规 |
| 右侧滑块 | 点击与拖动都能跳转，拖动时显示序号 |
| 调用系统查看器 | 图片与视频均交给系统应用打开（当时尚无内置查看器；现在该路径作为显式操作与失败回退保留） |
| 无可用应用 | 禁用查看器后提示「系统没有可以打开 X 的应用」 |
| 缩略图 | 图片与视频缩略图均正常渲染 |

### 本轮发现并修复的缺陷

| 编号 | 问题 | 修复 |
| --- | --- | --- |
| D1 | 一次完全复用缓存的扫描后，目录列表缓存不再失效，「重新扫描」再也看不到新增文件——实测 4ms 返回 823 条却什么都没读，只有重启应用才恢复 | 每次扫描开始时清空列表缓存；该缓存只用于避免同一次扫描重复列目录 |
| D2 | 视频拍摄时间从未读出，全部按文件修改时间排序 | `METADATA_KEY_DATE` 同时接受扩展与紧凑两种 ISO-8601；并把 MP4 的 1904 纪元当作「未设置」而不是日期 |
| D3 | 滑块标签被轨道的 34dp 宽度约束并溢出屏幕，只剩首字符 | 允许标签超出轨道宽度，改为靠右向左展开 |
| D4 | 拖动超过 900ms 后标签自行消失，手指仍在移动 | 计时随标签更新重启，拖动期间保持显示 |
| D5 | 只有违规列表变化时索引不写回，持久化的违规信息会过期 | 违规变化也写回索引 |
| D6 | `encodeDefaults=false` 让 `version` 字段从不写入，索引版本校验永远不触发 | 版本字段始终编码；缺少该字段的旧文件按旧版本处理并被丢弃重扫 |

D6 是 D2 的修复一开始不生效的原因：旧缓存不会被判定为过期。六项修复后均已在本轮模拟器上复验。

## 当前不做

不实现底层文件改名、移动、删除、覆盖或其他编辑；不读取 `待分类/`；不做后台全盘扫描、自动识别整理、复杂关系、阅读进度，也不为不常见格式引入额外解码库。

内置查看器不再属于本节：它已于 2026-09-20 重新确认，规则见 [AGENT_PRODUCT_RULES.md](../AGENT_PRODUCT_RULES.md) 第 5.1 节。

## 验证证据

- 单元测试：`./gradlew :app:testDebugUnitTest`，9 个测试类 70 个用例，0 失败（本轮实际运行结果）。`ui/UiStateTest.kt` 覆盖画集树的推导：层级可见性、同层不混排、五种排序、面包屑、空文件夹、计数含下层。新增 `ui/ViewerStateTest.kt`（10 个用例）固定查看器的纯决策：空列表不开查看器、越界索引钳制、相邻页边界、位置文案、播放器随前后台的创建/释放/暂停、错误码到可读文案。此前已有的 `media/VideoTimestampTest.kt` 固定视频时间解析的两种格式与 1904 纪元，`model/IndexTest.kt` 固定索引版本必须被写入且旧格式缓存不被信任。
- 构建：`./gradlew :app:assembleDebug` 通过且无编译警告。
- 真机：`baff50eb`（小米 14 / 23127PN0CC，Android 16 / API 36，arm64-v8a），WSL 侧工具链构建并 `adb install` 安装后完成 `.nomedia` 开关验证与内置查看器的实现验证（见上文两节）。查看器结论取自设备上的 `cache/logs/rem.log`。
- 模拟器（本轮新增，Linux 侧）：`RemWSL`，Android 16 / API 36，x86_64，KVM 加速。用于画集多级目录、排序、扫描进度与增量落盘的验证：合成库 316 文件（相册 200、三个项目含两层嵌套、一个空文件夹、一个混合层、故意放入的违规项），经 SAF 选择器接入。
- 旧模拟器：`Rem_API_36`，Android 16 / API 36，1080x2400（Windows 侧，本轮未再使用）。
- 上表每一行均由本轮界面操作加 `adb` 查询得出，历史数字不作为本轮证据。
- 探针实测数据来自仓库外的探针工程（`dev.susnowy.remprobe`），**不是**本仓库的构建产物，无法用本仓库的命令复现；本仓库自身的查看器结论已按上一条由真机日志复核。

## 未验证

真实 Library 的 SAF 接入规模（本轮为合成 Library 加用户提供的探针 Library，非完整真实库）、EXIF/视频元数据在各厂商设备上的完整覆盖，以及小米 Gallery 界面对已有媒体的即时隐藏/恢复表现，仍需独立场景验证。

其中 MKV/WebM 不通过 `METADATA_KEY_DATE` 暴露日期，这两类视频按修改时间排序，符合规则但未逐容器确认；滑块的拖动是在模拟器上验证的，未覆盖真机触控差异。内置查看器的未验证项单列在「内置查看器」一节，不与上述条目混用。

本轮在 WSL 上重建了构建工具链（JDK 17 + Linux 版 Android SDK），`./gradlew` 的测试与打包均在该环境完成；此前的构建发生在 Windows 侧，两个环境下 debug 包体积不同（22.4 MB 与 20.9 MB），体积数字不作为证据使用。

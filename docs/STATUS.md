# Rem 当前状态

## 规范基线

2026-09-20 已根据用户确认重置产品规范。当前唯一有效的详细规则见 [AGENT_PRODUCT_RULES.md](../AGENT_PRODUCT_RULES.md)，产品目标摘要见 [PRODUCT.md](PRODUCT.md)。

## 当前目标

- 根目录固定为 `相册/`、`画集/`、`待分类/`、`.gallery/`。
- 相册直接存放图片和视频，使用拍摄时间排序，缺失时回退到文件修改时间；地点只读取已有媒体元数据。
- 画集使用 `作者名称-项目名称/`，内部媒体按四位序号从 `0001` 开始；不规范文件排在规范序号文件之后。
- 接入、刷新和读取范围都必须显式限定；`待分类/` 完全不处理。
- 支持混合或分开查看、最小字符串匹配、长列表右侧滑块和系统查看器/播放器。

## 实现状态

本轮只完成规范文档重置，没有执行代码重构、测试或构建。现有代码不应被视为已经符合新规范；后续实现必须从上述目标重新建立最小验证闭环。

## 清理结论

旧的 Tag、Collection、复杂关系模型、自动识别整理、阅读进度、复杂页面跳转、后台全盘扫描、内置播放器和未来文件编辑的预置逻辑均不再作为当前需求依据。后续若需要，必须重新提出并确认。

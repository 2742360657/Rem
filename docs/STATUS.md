# Rem 当前状态

更新日期：2026-09-19。代码核对基线：4c5d83a 加本轮 R03/R07 系统相册筛选与定位。本文是当前进度入口；产品要求见 [PRODUCT](PRODUCT.md)，历史证据见 [DEV_LOG](DEV_LOG.md)。

本轮接续 R07 阅读体验与 R11 加载性能，未改 Schema、未访问真实 Library。真机已由用户移除，当前仅模拟器在线；当前验证与历史证据分列如下。

## 1. 版本与验证基线

- 最新接入校验模块（abe0525 加登记前校验）：305 单测、lint、debug/test APK 和 API 36 十二项接入/SAF 设备测试通过；随后补充有效 catalog 恢复后的重试断言，接入校验测试再次通过。测试 Context 仅模拟目录权限预检，实际元数据走测试 Provider；不是系统选择器真机授权验收。

- 最新归档方向模块（c5add9d 加 EXIF）：305 单测、lint、debug/test APK 通过；API 36 四项归档测试后，再加入全部八种方向映射测试，九项归档/方向/恢复测试通过。实际 JPEG/ZIP 覆盖旋转后尺寸、像素方向及缓存；无本轮全量设备或真机证据。

- 最新归档尺寸模块（33f10dc 加尺寸回调）：305 单测、lint、debug/test APK 和 API 36 九项归档/页面恢复/阅读位置设备测试通过。合成 ZIP 验证解码尺寸回调，挂起像素结果的实际页面组件验证加载与最终高度一致；没有本轮全量设备/真机结果。

- 最新归档取消模块（6f007d4 加取消读取）：Windows / Java 17，305 单测、lint、debug/test APK 与 API 36 八项归档身份/页面恢复/阅读位置设备测试通过。新增三项流单测覆盖取消前不读取、读取返回后取消不交付数据且关闭流、正常缓冲读取；非真机阻塞时长验收。

- 最新预加载模块（1faf8d2 加逐页容错）：Windows / Java 17，302 单测、lint、debug/test APK 和 API 36 八项漫画位置/恢复/布局设备测试通过。新增三项单测覆盖故障隔离、两路并发、排队取消和取消异常传播；没有本轮全量设备或真实介质结果。

- 最新尺寸布局模块（d26625e 加普通页尺寸预读）：Windows / Java 17，299 单测、lint、debug/test APK、API 36 全量 72 项设备测试通过；再扩充横图/长图场景后，三项延迟解码布局测试通过。尺寸测试覆盖 EXIF 旋转、流关闭、失败回退及取消传播，布局测试证明已知比例后加载与成功高度一致。无本轮真机或真实介质性能证据。

- 最新页面恢复模块（d1fae4c 加漫画重试）：Windows / Java 17 的 299 单测、lint、debug/test APK 和 API 36 五项漫画设备测试通过。三项新增覆盖缺失图片恢复后同请求重试、归档重试加载提示、页面身份切换清旧图与退出取消；另两项为实际阅读位置/跳转/手势回归。没有本轮真机或全量设备结果。

- 最新缓存模块（dba6837 加归档位图身份修复）：299 单测、lint、debug/test APK 和 API 36 六项漫画/归档/预览设备测试通过；随后补同来源命中断言，三项归档测试再次通过。已复现并修复跨库同路径串页及大小变化复用旧页，合并 Edition 按实际容器版本解码。本模块未重新运行全量设备套件，上一统一基线 63 项结果保留如下。

- 最新统一基线（4c5d83a 加系统相册模块）：Windows / Java 17，299 单测、lint、debug/test APK、API 36 模拟器全部 63 项设备测试通过。新增 150 项合成系统媒体的类型/来源组合过滤、独立位置、选择集、加载空快照和重建恢复；无真机或真实相册/Library 验证。以下均为此前阶段证据。

- 最新统一基线（a53ed65 加解码测试）：Windows 299 单测、lint、debug/test APK 构建及 API 36 全部 60 项设备测试通过。测试端 MediaCodec 生成 160×120、30 帧 AVC/MP4，应用同款播放器/实际 Surface 渲染首帧、seek 到 1 秒、横屏全屏后播放到结束。真机、真实长视频、多格式与完整剧集导航仍未验收。

- 最新调度模块：Windows 完整单测/lint/debug 与 test APK 构建通过；API 36 的 ScanPriority、SAF 与 OfflinePreview 共 11 项设备测试通过。上轮管理列表的 55 项全量设备结果不冒充本次全量结果。

- 最新管理列表模块：Windows / Java 17，298 单测、lint、debug/test APK 构建、API 36 模拟器全量 55 项设备测试通过。新增覆盖 Inbox 三个路径分区的独立位置及重建、已忽略媒体选择恢复、分组/系列定位不修改草稿；未作真机或真实介质验证。

- 最新本轮：Windows / Java 17，298 单测、lint、debug/test APK 构建及 API 36 模拟器全部 48 项设备测试通过。新增实际漫画组件场景覆盖像素偏移重建、同页跳转、末页不误完成、真实滑动完成及自动下一话；冷归档并发测试覆盖离线预览让路。未验证本轮真机、真实介质或完整产品验收。

- 一个 Android module 加独立 JVM library-tool；release com.susnowy.rem，debug com.susnowy.rem.debug。
- 本轮 Windows / Temurin Java 17：桌面工具 12 单测、Android 281 单测通过；lint 0 error、31 warnings、1 hint；debug/test APK 及 installDist 构建通过。说明补回凭据保护后，桌面 12 单测和 LibraryManager 13 单测再次通过，API 36 模拟器 SAF 类 9 测试通过（含 Agent 字段编辑读回）。独立 Windows 启动脚本在 1 Work / 1 缺失 Asset 的合成小库完成 validate、preview、apply、export-guide 和读回；未做本轮真机或真实库验证。
- 便携 format=gallery-library，Schema v4；本机数据库 v11。
- 2026-09-18 缺失媒体语义拆分记录：279 单测、28 设备测试通过，lint 0 error，debug 与 test APK 构建通过。证据：DEV_LOG「区分媒体不在本机与需修复，并补设备测试」及提交 082cf8d / 18f07b8；测试环境记录为 API 36 模拟器，不外推为真机全库验证。
- 后续视频空来源守卫：提交 05203ce，记录见 DEV_LOG 首批同日条目；不把前一轮全套测试自动视为这一提交的新增验证。
- 模拟器已走查：5 章 Series 顺序、继续、完成/重读、自动衔接、目录图集、缩放、跨行拖动和边缘滚动；ArchiveCache 设置统计与清除。
- 2026-09-17 小米 23127PN0CC 小库走查：Inbox、Group/Series 编辑及批量关系操作、Edition 深比较、虚拟合并及计划阅读。证据见 DEV_LOG 当日对应条目。
- 真实库切片 + Android 16 模拟器：26 个便携文档、20 个完整媒体作品，340 文件约 1.27 GB；1352 个 Work 进入投影，20 个媒体在本机、1332 个缺失；909 项批量追加标签写入及便携字段保留已记录。
- 全量真实可移动介质直连手机的扫描、分页需求、大量视频预览和持续真手指体验：未验证。用户此前暂不安排全库手机测试，不把它当继续文档或工具工作的阻塞。

## 2. 需求落实表

编号及验收只在 PRODUCT 定义。这里的“部分”明确意味着需求尚未完成，不因已有相似功能就结项。

| 需求 | 当前落实程度与证据边界 | 剩余工作 |
| --- | --- | --- |
| R01 | 目录/ZIP/CBZ 阅读、页序、Series 关系已实现；小库/模拟器有阅读证据 | 大归档及真实介质长时间阅读 |
| R02 | 漫画 Series 话列表/编号/连续阅读已验证；视频播放及位置保存已实现 | 视频剧集连续观看全流程单独复核；声明支持容器，不能泛称所有常见格式 |
| R03 | 系统相册、复制导入、照片/视频与拍摄信息补全已实现；系统相册组合筛选、序号定位及各筛选位置恢复有模拟器证据 | 时间回退、Library 内混排/过滤及完整返回上下文仍需验收 |
| R04 | 目录、Collection/Tag 查询和 Group 编辑已有 | 多级分类浏览及往返体验按需求走查，未实现部分再拆小任务 |
| R05 | 作者元数据/查询、独立视频、派生混合组及保存 Group 已有 | 作者→系列/独立作品统一入口、完整一期混合浏览验收；不声称任意混合 Edition 已可播放 |
| R06 | 已确认，待实现验收 | 范围内随机图片页、来源回溯、完整一期及随机上下文返回 |
| R07 | 漫画完成/继续/重读/缩放和系列衔接已验证；视频缺失提示已修复 | 真机持续手势、视频系列上下文、密度/返回/解码前宽高比 |
| R08 | 批量追加/替换/清空作者、Tag、Collection 和统一归属已实现；字段冲突跳过、人工空值锁、分库结果有单测与模拟器证据；批量移出分组/系列已实现并保留关系规则 | 冲突报告覆盖度；新字段和关系编辑真机走查 |
| R09 | inventory/enrich 分离、弱识别、Inbox 和人工锁已有测试/走查 | 持续限制识别成本；新增规则必须有独立证据与测试 |
| R10 | 完整规则与契约随 APK/桌面工具打包；独立 v4 校验、已有 Work 字段编辑、外部备份、冲突拒绝、显式恢复和说明单独导出已有单测及模拟器 SAF 读回证据 | 新建实体、Group/Series/Edition 关系编辑及多文档事务仍未支持；真实介质和跨平台分发待验收；不能称已实现无人值守整理闭环 |
| R11 | 便携 v4、恢复写入、catalog-only 投影、missingMedia 拆分有测试/切片证据 | 无媒体遍历前的冷接入可用性及耗时单独验证；全库跨设备和可移动介质性能待用户安排 |

## 3. 已知工程边界

- R11 接入在登记前加载验证 catalog/state/Inbox，相关错误不再仅写日志后返回成功。损坏 catalog 不登记、损坏字节保留、修复后重试成功有设备证据。数据库投影失败仍可能留下登记，完整投影事务及无扫描冷接入作品可见性需继续审查；原手机“无反应”根因未据此关闭。

- R07 归档 EXIF 方向已归一化：八种方向旋转/镜像，尺寸回调及降采样目标按显示坐标处理。替代此前归档方向未支持的记录；新增一次条目头部读取，通常命中本机归档缓存，慢介质首次读取性能仍待实测。

- R07 归档页已接入原有 bounds 阶段的尺寸回调，不额外读取整页或遍历归档；页面保存已知比例，加载与成功同高。此前“归档尺寸未接入”的历史记录已由此替代。首次未知尺寸占位变化仍存在；归档 BitmapFactory 路径尚未归一化 EXIF 旋转，不能外推普通页 EXIF 测试。

- R11 归档 bounds 取消不再按失败记录；条目流 read/skip 前后及尺寸/像素/缓存阶段之间检查取消。JVM 流测试及正常实际归档解码回归通过，尚未注入 Android 原生解码过程中的取消，不能证明原生或 Provider 同步调用立即中断。

- R07/R11 相邻页预加载普通异常不再终止视口观察与同批其他读取；取消继续传播，排队任务取消后不启动。可见页仍自行显示失败和重试。Provider 同步调用、预读与可见页的优先级细化及归档尺寸预留仍待完善。

- R07 普通页现按进入组合的页面在 IO 读取尺寸与 EXIF，并保存已知比例，在像素解码前预留高度。没有为此预读整本漫画。首次获取尺寸前的占位仍会变化，归档页尚未接入；额外头部读取对慢 Provider 的影响待实测，尚不能宣称连续阅读完全无跳动。

- R07/R11 普通漫画图片页支持 Coil 请求 restart；归档页异步状态按来源身份及重试次数隔离，取消不作为失败显示。测试覆盖实际图片请求和可控异步归档组件；同步 Provider 调用仍不能保证立即中断，固定占位尺寸与解码后布局变化尚待改善。

- R07/R11 归档位图缓存按 Library、容器路径/大小/时间戳、条目和解码尺寸隔离；合并图集采用实际容器元数据。合成 SAF 归档像素验证通过；外部更新仍依赖存储元数据刷新，大小和时间戳均未改变的字节替换不靠本缓存识别。漫画解码前宽高比与加载布局稳定性仍待完善。

- R03/R07 系统相册支持来源与媒体类型组合筛选、按当前结果序号定位、分别保存位置；刷新加载期间保留选择并禁用导入，完成后清理已不可访问选择。此轮未访问 MediaStore 真实内容，不外推为真实相册导入验收；其他分类/设置长界面仍需补齐。

- R04/R07 分组书架现支持内容序号定位，跳过说明和分区标题，状态持有者保留在空内容分支外。完整单测/lint/APK 和 API 36 八项共享定位设备测试通过；新增 150 手动+1 派生分组的第 100 项、空快照重建及末项验证。系统相册和其他分类/设置长界面仍未全部覆盖。

- R07 合成视频实际解码已取得模拟器证据，补足此前仅 WAV/未准备来源的覆盖；小尺寸无音轨 AVC 并不代表所有容器/编码或音画同步。完整多集 UI 上下文与便携进度读回仍需专项场景。

- R07 音频中断：播放器统一按媒体/电影音频用途配置，启用 Media3 焦点处理及耳机断开暂停。完整单测/lint/APK 与 API 36 五项播放器回归通过；新增合成 WAV 实播后由 AudioManager 永久焦点竞争触发暂停及原因回调。耳机物理拔插、蓝牙、电话和真实视频解码未验证；模拟广播被系统安全边界拒绝，没有绕过也不计为通过。

- R07 视频生命周期：后台暂停并通过原进度接口保存，返回只恢复原播放意图，非活动页保持暂停。Windows 完整单测/lint/两个 APK 构建、API 36 四项播放器设备测试通过，包含实际 MainActivity 停止/恢复与既有全屏/完成回归。生命周期测试使用未准备的视频来源，证明控制与位置回调，不证明真视频解码、音频焦点或真实介质进度落盘。

- R11：媒体补全纯读取阶段可被前台抢占后重试，扫描在下一目录查询前等待，哈希分块响应取消；提交留在重试块外。阻塞流单测验证取消关闭流、重试摘要正确且仅交付一次，SAF 设备测试验证等待期间未查询 Provider、取消不成为 unreadable。同步 Provider 调用仍不能立即中断，Coil 直接读取尚未纳入；真实介质性能待验证。

- Inbox 三个路径列表、分组编辑器和系列编辑器已支持定位；Inbox 分区使用各自保存的状态，已忽略媒体选择可恢复。完整 55 项模拟器回归通过。分组书架、系统相册、部分分类/设置等长界面仍待补齐。

- R07/R08 快速定位扩展到章节列表、添加成员、选择分组/系列；按当前筛选结果定位，不改变选择集。298 单测、lint、两个 APK 构建及 API 36 新增 3 项实际界面测试通过（150 项列表、章节返回锚点、过滤后选择保留、目标身份）。其他长界面尚未全覆盖，不能称“所有列表已完成”；此前 48 项设备回归属于漫画修复基线。

- R07 漫画恢复现在使用已初始化列表的精确位置，修复 Coil 初始 Empty 零高度导致跳页落错位置；末页跳转按实际钳制布局结束，完成基线与跳转隔离。两项聚焦设备测试及完整 48 项模拟器回归通过。测试退出曾卡在模拟器 GPU 管道，改软件后端/关闭 Vulkan 并等待滑动动画结束后通过，过程见 DEV_LOG；未外推真机体验。

- R07/R11 离线预览让路的历史基线：2026-09-19 完整工程检查及 API 36 全部 46 项设备测试通过；冷归档并发回归验证取消后让出复制锁、预览恢复与半成品清理。扫描/补全的后续覆盖见本节最新调度条目，未作真机或真实介质验证。

- 漫画页列表加载增加说明、取消与错误/空页原地重试；取消异常不再当作普通失败。完整工程检查和 API 36 两项重试/取消设备测试通过。测试使用可控异步加载，不承诺能立即中断 Provider 同步调用；调度覆盖范围见本节最新条目。

- 虚拟 Edition 页计划解析已切到 IO，修复根目录图片父路径解析，并保留 Provider 查询错误。完整单测/lint/APK 构建及 API 36 两项解析设备测试通过（从 Main 调用确认回调不在主线程、每目录一次、顺序保留、权限失败传播）；这不是慢介质前台调度的完整验收。

- 前台加载首项修复：ArchiveCache 慢速 Provider 复制不再持有缓存访问锁，已缓存作品和统计可并行返回。295 单测、lint、构建及 10 项 SAF/预览设备回归通过；新并发测试使用被主动阻塞的输入源验证。未缓存作品仍串行复制，尚未完成整体前台优先调度，也不能据此关闭真机长时间转圈反馈。

- R07 视频全屏/横屏已实现，同一播放器切换显示，退出恢复方向。完整工程检查及 API 36 全部 42 项设备回归通过；新增实际 MainActivity 全屏交互测试检查实例、位置和方向恢复，未验证真机长视频解码。

- R07 返回位置新增证据：API 36 模拟器 150 项共享 MediaGrid，跳至第 100 项后进入/退出详情、列表暂空期间重建再加载，均恢复同一网格行首。测试使用应用同款 SaveableStateHolder，证明共享网格路径，不能据此关闭所有页面或真机“返回顶部”反馈。

- R07 快速定位首批：漫画底部页码可点击跳页，共享媒体网格支持位置输入及首末快捷选择；完整工程检查及 2 项输入边界设备测试通过。实际漫画末页跳转/完成语义、返回滚动位置仍需场景回归，其他长列表的定位入口尚未覆盖。用户明确希望 EhViewer 同类高质量漫画体验并避免复杂低频功能，已纳入 PRODUCT；真机已移除，后续先用模拟器持续测试。

- 2026-09-19 用户真机旧包反馈已纳入 PRODUCT：视频横屏/观看优化、各长界面快速定位与页码跳转、返回滚动位置、本地内容加载优先级、混合目录视频误跳图集。真机 baff50eb 已在线；包更新时间仍为 00:00:23。日志显示补全 1261 项中失败 1259 项，Provider 单次最慢可达 51 秒，需查来源定位与调度，不能仅归因于缩略图。S 盘尚未在当前进程卷查询中识别；不访问或改动原库。
- 当前视频系列衔接已实现，纯队列及现有 37 项模拟器设备测试通过；真实多集视频解码与返回场景仍待新测试覆盖。

- R07 视频完成判定已改为播放结束事件，防止五秒内短片被误记完成、加载旧进度前退出覆盖位置。新增纯状态测试和 API 36 一秒合成 WAV 的 ExoPlayer 实播测试通过；视频解码/剧集连续观看仍待独立场景验收。
- R08 两模块提交后完整模拟器设备回归 36 项通过（2026-09-19，API 36）；后续视频修正另跑全套单测/lint/构建和一个新增播放器设备测试，不能把此前 36 项称作视频修正后的全量回归。

- 2026-09-19 R08：Windows / Java 17 全套单测、lint、debug/test APK 构建通过；API 36 模拟器新增 3 项批量界面/仓储测试通过。验证人工空值、并发目标冲突、无关字段保留、离线库独立失败，以及来源/版本/系列不被字段编辑重写。真机当前不在线，未进行本轮真库编辑。批量草稿界面重建后重置；跨库非整体事务。
- R11 真机补充证据：旧包日志 2026-09-18 22:52 曾报 identity 缺失，23:16 扫描 16 项成功；d9f2324 debug 已于 2026-09-19 00:00 覆盖安装。仍需新包现场复现，不能确认原始问题关闭。

- R11 接入反馈修正：目录授权与名称查询移到 IO，接入阶段显示独立进度对话框，失败保留错误及重新选择入口；持久授权失败不再忽略。用户报告“选目录确认返回后无变化”，API 36 模拟器真实系统选择器接入合成空目录成功，尚未取得用户真机日志，原始故障根因仍待现场确认，不能视为已解决。
- 本轮接入反馈验证（2026-09-18，Windows / Java 17）：281 单测、lint 0 error、debug/test APK 构建通过；API 36 模拟器接入反馈与 SAF 共 11 测试通过，具体命令与边界见 DEV_LOG 同日接入条目。

- 初次 inventory 是一次原子遍历，提交前中断要重来；只有 enrichment 可恢复。
- refreshFromDatabase() 仍物化整个媒体表；分页/清点 checkpoint 的取舍以实际规模证据决定。
- 可显示的某些图片格式无法由 BitmapFactory 生成离线 JPEG。
- deep compare 需显式读取内容；像素级重编码匹配未实现。
- 混合、多 Edition、单 Series 的运行时 MediaItem 投影比便携模型窄；不得因 JSON 能存就宣称 UI 能完整消费。
- v4.json 是格式概要，不是完整验证器。App 的进程内 mutex 不能保护外部 Agent 的同时写入。
- 库内 GALLERY_LIBRARY.md 由构建时打包的完整规则与契约生成；已有库接入不覆盖用户版本，export-guide 仅另存候选说明。桌面工具只编辑已有 Work 的单份 catalog，发现恢复槽或物理事务记录时保守拒绝普通编辑；同库必须单写入者。校验不表示媒体可达，也不是完整 JSON Schema 验证器。
- 缺失媒体不同于需修复：catalog-only 投影用 missingMedia=true、size=0、uri 为空、inInbox=false；已索引后异常失联可以 needsRepair=true。这是对旧 AGENTS / 早期 DEV_LOG 描述的现行替代。
- 回收站期限仅提醒，不自动永久删除；部分删除显式重试剩余来源，已删字节不能恢复。
- 任意新实现要保留字段级并发合并、稳定 ID 和可恢复便携写入，不能为简化 UI 回退这些机制。

## 4. 真实库与环境证据

- 2026-09-17 只读样本：约 464.92 GiB / 76,517 文件；具体格式分布留在历史记录，非程序常量。
- 2026-09-18 切片实验的源库：88,736 文件；旧日志写约 492.1 GB，而 v2 草案写 492.09 GiB，单位未核定。当前不据此换算或宣称精确容量；目录数 1,149 及分目录体积为草案来源、尚未复核，不作验证基线。
- 历史约 360 MB/s 是大视频顺序拷贝观测，不代表 SAF 小文件、归档和哈希速度。
- 主机可为 Windows 或 Linux；盘符、挂载路径、卷标可变。现场寻找 Library root 下的 .gallery/library.json；Android 使用用户授予的 SAF 目录。
- 测试优先用独立小库或有备份的切片；不要为了延续历史测试自动访问或修改真实源库。

## 5. 下一步顺序

1. R10：已有 Work 字段编辑与规则分发的小库闭环已完成；后续独立设计新建实体、关系整理及多文档安全提交，不扩大当前工具的写入范围。真实库使用前仍需介质外完整备份与切片验收。
2. R08：补批量追加/替换/清空/统一设置，复用领域规则；优先验证人工空值、并发修改、冲突反馈和跨库分开提交。
3. R03/R04/R05/R02：按六类媒体建立小而完整的场景 fixture，先验收已有入口，修补作者聚合、分类、混合与影视上下文差距。
4. R06：实现限定范围的随机图片发现→完整一期→返回，保留来源身份；R07 同步做局部阅读交互打磨。
5. R11：用户安排真实介质后复核全量性能及跨设备体验，再决定 checkpoint / paging 的具体方案。

此顺序是执行计划，不是新增产品优先级；安全和便携数据缺陷出现时优先修复。需求补充可调整顺序，但必须保留未完成条目。

## 6. 状态更新规则

每次影响需求的任务更新对应行和证据，标明代码实现、单测、模拟器、真机、切片或全库。完成必须满足 PRODUCT 验收，不能只以“代码存在”结项；旧验证不得冒充当前提交的新结果。

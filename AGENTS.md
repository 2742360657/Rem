# Rem Agent 开发协议

## 唯一有效入口

开始任何任务前只读取：

1. [AGENT_PRODUCT_RULES.md](AGENT_PRODUCT_RULES.md)：当前产品和文件模式；
2. [docs/PRODUCT.md](docs/PRODUCT.md)：产品目标摘要；
3. [docs/STATUS.md](docs/STATUS.md)：当前实现状态和未完成事项；
4. 相关代码和测试。

`docs/USER_GUIDE.md` 只在修改用户可见操作时读取。其他旧文档已删除，不得从 Git 历史或旧提交自行恢复产品要求。

## 开工声明

每项任务开始时说明：目标、范围、非目标、将修改的文件、是否影响 Library 文件、验收条件和验证命令。目标不清或要求互斥时先提问；普通实现选择由 Agent 按当前产品规范做最小决定。

## 目标边界

当前只做：显式接入 Library、读取规定目录、媒体时间排序、已有地点显示、作者/项目名称最小匹配、图片视频混合或分开浏览、长列表右侧滑块、调用系统查看器或播放器。

当前不做：全盘或后台扫描、读取 `待分类/`、底层文件改名/移动/删除/覆盖、任何文件编辑、自动识别和整理、Tag、Collection、复杂关系模型、阅读进度、复杂页面跳转、内置备用播放器，以及为未来功能预建复杂基础设施。

## Library 边界

根目录只允许：

```text
Library/
  相册/
  画集/
  待分类/
  .gallery/
```

扫描只能由用户显式触发，只读取 `相册/` 和 `画集/` 的规定层级。`待分类/` 不扫描、不索引、不展示、不整理。原始媒体优先，索引必须可重建。

## Agent 协作

默认单 Agent。只有任务可以独立验收且文件归属不重叠时才并行。主 Agent 负责拆分、接口、整合和最终验证；共享规范只能由主 Agent 修改。子任务必须返回变更清单、验证命令、结果和未决问题。

Agent 不得因为“未来可能需要”扩展需求。发现旧代码或旧文档与当前规范冲突时，登记为待清理差距并停止沿用，不把旧逻辑包装成新功能。

## 修改与验证

保持用户已有修改，不使用破坏性 Git 操作。文档变更检查链接、目标覆盖和 `git diff --check`；代码变更先跑聚焦测试，再按需要运行完整构建。未验证的行为必须明确标为未验证，不得把旧测试数字当作当前证据。

底层文件变更若未来重新提出，必须先定义范围、预览、冲突检查、确认、恢复和结果验证；当前任务不得实现。
